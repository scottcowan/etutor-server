---
phase: 04.1-knowledge-corpus-and-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/corpus/raw/index.md
  - docs/corpus/wiki/index.md
  - docs/corpus/wiki/sources/happiness-in-slavery-markradulich.md
  - docs/corpus/wiki/sources/edtech-research-raz-kids-raena.md
  - docs/corpus/wiki/sources/uk-book-bands-methodology-research.md
  - docs/corpus/wiki/experiments/bernoulli-flight.md
  - docs/corpus/wiki/experiments/bread-baking-science.md
  - docs/corpus/wiki/experiments/candle-science.md
  - docs/corpus/wiki/experiments/lemon-battery.md
  - docs/corpus/wiki/experiments/magnetic-field-mapping.md
  - docs/corpus/wiki/experiments/pool-chemistry.md
  - docs/wiki/source-material/ (removed)
  - docs/experiment-guides/ (removed)
autonomous: true
requirements: [CORPUS-02]

must_haves:
  truths:
    - "Every existing source-material and experiment-guide file lives at its new docs/corpus/wiki path with git history preserved and no duplicate copy left at the old path"
    - "Every migrated file has frontmatter matching the D-10 (source) or D-07 (experiment) schema and topic_ids that exist in services/curriculum.py"
  artifacts:
    - path: "docs/corpus/raw/index.md"
      provides: "L0 catalog stub per D-04"
    - path: "docs/corpus/wiki/index.md"
      provides: "Master index of all L3 pages (D-04) — placeholder table regenerated by later plans"
    - path: "docs/corpus/wiki/sources/happiness-in-slavery-markradulich.md"
      provides: "Migrated L3 source page with D-10 frontmatter"
    - path: "docs/corpus/wiki/experiments/bernoulli-flight.md"
      provides: "Migrated L3 experiment page with D-07 frontmatter added"
  key_links:
    - from: "docs/corpus/wiki/experiments/*.md frontmatter topic_ids"
      to: "services/curriculum.py CURRICULUM topic ids"
      via: "manual cross-check during migration"
      pattern: "topic_ids:"
---

<objective>
Scaffold the `docs/corpus/` directory structure (D-04) and migrate the three existing `docs/wiki/source-material/` files and six `docs/experiment-guides/` files into it. This is pure content-migration/authoring work — no MCP server code in this plan.

Purpose: Establish the filesystem layout every later plan (MCP indexing, tool functions, pilot content) reads from, and close the CORPUS-02 gap for existing content that predates this phase.
Output: `docs/corpus/` tree populated with migrated, correctly-tagged L3 source and experiment pages; old directories removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-RESEARCH.md
@services/curriculum.py
@docs/wiki/source-material/happiness-in-slavery-markradulich.md
@docs/experiment-guides/bernoulli-flight.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold docs/corpus/ directory structure</name>
  <files>docs/corpus/raw/index.md, docs/corpus/source/.gitkeep, docs/corpus/summary/.gitkeep, docs/corpus/wiki/topics/.gitkeep, docs/corpus/wiki/people/.gitkeep, docs/corpus/wiki/sources/.gitkeep, docs/corpus/wiki/experiments/.gitkeep, docs/corpus/wiki/index.md</files>
  <read_first>.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-CONTEXT.md (D-01 through D-05)</read_first>
  <action>
Create the directory tree exactly per D-04: `docs/corpus/raw/`, `docs/corpus/source/`, `docs/corpus/summary/`, `docs/corpus/wiki/topics/`, `docs/corpus/wiki/people/`, `docs/corpus/wiki/sources/`, `docs/corpus/wiki/experiments/`. Add `.gitkeep` to any directory that would otherwise be empty after this task (source, summary, topics, people — these are populated by later plans or left for the content backlog).
Write `docs/corpus/raw/index.md` as an L0 catalog stub: a markdown table with columns `Source | URL | Author | Captured | Promoted to` — one row per file that will be migrated in Tasks 2 and 3 of this plan (9 rows total: 3 source-material entries, 6 experiment-guide entries), with the "Promoted to" column pointing at the new `docs/corpus/wiki/...` path each will land at.
Write `docs/corpus/wiki/index.md` as the master L3 index per D-04: a markdown table with columns `Path | Type | Topic IDs | Level`. Populate one row per file migrated in Tasks 2-3 of this plan. Note at the top of the file, as an HTML comment, that this index is a human-readable table of contents regenerated by tooling, not the MCP server's authoritative index (the server always globs the filesystem directly — see 04.1-RESEARCH.md Anti-Patterns).
  </action>
  <verify>
    <automated>test -d docs/corpus/wiki/topics && test -d docs/corpus/wiki/experiments && test -f docs/corpus/wiki/index.md && test -f docs/corpus/raw/index.md</automated>
  </verify>
  <done>docs/corpus/ tree exists with all 7 subdirectories and the two index files populated with stub rows for the 9 files about to be migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate docs/wiki/source-material/ to docs/corpus/wiki/sources/ (git mv, frontmatter validation only)</name>
  <files>docs/corpus/wiki/sources/happiness-in-slavery-markradulich.md, docs/corpus/wiki/sources/edtech-research-raz-kids-raena.md, docs/corpus/wiki/sources/uk-book-bands-methodology-research.md, docs/wiki/ (removed)</files>
  <read_first>docs/wiki/source-material/happiness-in-slavery-markradulich.md, docs/wiki/source-material/edtech-research-raz-kids-raena.md, docs/wiki/source-material/uk-book-bands-methodology-research.md, 04.1-CONTEXT.md D-10</read_first>
  <action>
For each of the 3 files in `docs/wiki/source-material/`, run `git mv docs/wiki/source-material/<file>.md docs/corpus/wiki/sources/<file>.md` to preserve history. These files currently have a "Curriculum Relevance" markdown table (level/subject/topic), not the D-10 YAML frontmatter block — this migration is a simple move per the CONTEXT.md D-05 note ("git mv") but each file still needs a YAML frontmatter block prepended matching the D-10 schema (title, type, source_url, author, captured, topic_ids, level, prerequisite_topics, curation_note) derived from each file's existing "Curriculum Relevance" table and header metadata (Source/Duration/Format/Captured lines). Map "Curriculum Relevance" table rows to `topic_ids` by finding the matching `services/curriculum.py` subject + closest topic name (e.g. happiness-in-slavery's "Political Systems" row maps to `authoritarianism`-family topic ids in that subject — grep `services/curriculum.py` for `"Political Systems"` and `"Manipulation"` to find exact ids). Set `type: video_essay` for the TikTok/YouTube-sourced file, `type: article` for the two research write-ups. Leave `prerequisite_topics: []` and `curation_note: ""` unless the file's own content already implies a specific prerequisite refresher (D-08).
After removing the last file from `docs/wiki/source-material/`, delete the now-empty `docs/wiki/` directory tree if nothing else lives under it.
Update `docs/corpus/raw/index.md` and `docs/corpus/wiki/index.md` (Task 1 stubs) with the final topic_ids and level for each migrated file.
  </action>
  <verify>
    <automated>test ! -d docs/wiki/source-material && grep -l "^topic_ids:" docs/corpus/wiki/sources/*.md | wc -l | grep -qx 3</automated>
  </verify>
  <done>All 3 files exist under docs/corpus/wiki/sources/ with git history preserved (git log --follow shows pre-move commits), each has a valid D-10 frontmatter block with topic_ids referencing real services/curriculum.py ids, and docs/wiki/source-material/ no longer exists.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate docs/experiment-guides/ to docs/corpus/wiki/experiments/ (git mv + author D-07 frontmatter)</name>
  <files>docs/corpus/wiki/experiments/bernoulli-flight.md, docs/corpus/wiki/experiments/bread-baking-science.md, docs/corpus/wiki/experiments/candle-science.md, docs/corpus/wiki/experiments/lemon-battery.md, docs/corpus/wiki/experiments/magnetic-field-mapping.md, docs/corpus/wiki/experiments/pool-chemistry.md, docs/experiment-guides/ (removed)</files>
  <read_first>docs/experiment-guides/bernoulli-flight.md, docs/experiment-guides/bread-baking-science.md, docs/experiment-guides/candle-science.md, docs/experiment-guides/lemon-battery.md, docs/experiment-guides/magnetic-field-mapping.md, docs/experiment-guides/pool-chemistry.md, 04.1-CONTEXT.md D-07</read_first>
  <action>
None of these 6 files currently have YAML frontmatter — each starts directly with a `# Title` heading followed by a `**Curriculum topic:** \`<id>\`` and `**Age:** ...` line. For each file: `git mv docs/experiment-guides/<file>.md docs/corpus/wiki/experiments/<file>.md`, then prepend a YAML frontmatter block matching the D-07 schema exactly: `topic_ids` (as a one-element list from the `**Curriculum topic:**` line — verify the id exists in `services/curriculum.py` CURRICULUM by grepping for it), `min_age` (parse the lower bound from the `**Age:** Y3-8 (ages 8-13)` style line — use the lower numeric age), `flagged_dangerous` (set `true` only for pool-chemistry.md and candle-science.md, since these involve chemicals/open flame — `false` for the other four; this is a judgment call per D-06's "genuinely dangerous activities" language, document the reasoning inline as a one-line YAML comment above the field), `equipment` (from the `**Equipment needed:**` line, split into a YAML list), `safety_notes` (a one-sentence summary of any safety-relevant instruction already in the guide body, empty string if none), `level` (200 for all six — these are hands-on KS2/KS3 practical demonstrations, not KS1 introductions or KS3 exam-prep depth).
Do not alter the existing markdown body content — frontmatter is prepended only.
After all 6 moves, delete the now-empty `docs/experiment-guides/` directory.
Update `docs/corpus/raw/index.md` and `docs/corpus/wiki/index.md` with final topic_ids/level/flagged_dangerous values for each of the 6 files.
  </action>
  <verify>
    <automated>test ! -d docs/experiment-guides && grep -l "^flagged_dangerous:" docs/corpus/wiki/experiments/*.md | wc -l | grep -qx 6</automated>
  </verify>
  <done>All 6 experiment guides exist under docs/corpus/wiki/experiments/ with git history preserved, each has a complete D-07 frontmatter block (topic_ids/min_age/flagged_dangerous/equipment/safety_notes/level) whose topic_ids reference real services/curriculum.py ids, original body content unchanged, and docs/experiment-guides/ no longer exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content author -> filesystem | Frontmatter and markdown body are hand-authored/migrated by a trusted local operator (no untrusted external input in this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | git mv history rewrite | accept | `git mv` is used explicitly (not `rm` + `Write`) specifically to preserve history and avoid silent content substitution; verified via `git log --follow` in task done-criteria |
| T-04.1-02 | Information Disclosure | flagged_dangerous mis-set on chemistry/flame experiments | mitigate | Explicit per-file reasoning required inline in frontmatter comment (Task 3) rather than a blanket default, so the hard safety gate (D-06) is deliberately set, not defaulted to false |
</threat_model>

<verification>
`git status` shows no files remaining under `docs/wiki/source-material/` or `docs/experiment-guides/`. `grep -rL "^topic_ids:" docs/corpus/wiki/sources/*.md docs/corpus/wiki/experiments/*.md` returns empty (every file has frontmatter).
</verification>

<success_criteria>
docs/corpus/ directory scaffolded per D-04. All 9 pre-existing content files migrated with git history intact and schema-correct frontmatter. Old directories removed. Index files reflect the migrated content.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-01-SUMMARY.md` when done
</output>
