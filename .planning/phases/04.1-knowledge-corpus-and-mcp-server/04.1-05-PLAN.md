---
phase: 04.1-knowledge-corpus-and-mcp-server
plan: 05
type: execute
wave: 3
depends_on: ["04.1-03"]
files_modified:
  - services/corpus_mcp/server.py
  - .mcp.json
  - docs/corpus/INTEGRATION.md
autonomous: true
requirements: [CORPUS-03, CORPUS-04]

must_haves:
  truths:
    - "An educator can start the MCP server locally and Claude Code discovers it via .mcp.json, then call any of the 5 tools and get a real result against the migrated corpus content"
    - "The Phase 5 device-sync integration point is documented concretely enough that api/sync.py can be extended without a corpus schema change"
  artifacts:
    - path: "services/corpus_mcp/server.py"
      provides: "FastMCP instance registering all 5 tools as thin decorated wrappers over 04.1-03's *_impl functions, run over stdio"
    - path: ".mcp.json"
      provides: "Project-scoped MCP server registration (D-11, resolves RESEARCH.md Open Question 3)"
      contains: "mcpServers"
    - path: "docs/corpus/INTEGRATION.md"
      provides: "CORPUS-04 integration point documentation for Phase 5 SYNC-03"
  key_links:
    - from: ".mcp.json mcpServers.etutor-corpus.command"
      to: "services/corpus_mcp/server.py"
      via: "python -m services.corpus_mcp.server invocation"
      pattern: "services.corpus_mcp.server"
    - from: "services/corpus_mcp/server.py tool decorators"
      to: "services/corpus_mcp/tools.py *_impl functions"
      via: "thin pass-through wrapper, index built once at module import via build_index"
      pattern: "@mcp_app\\.tool\\("
---

<objective>
Wire the tool functions from 04.1-03 behind the official `mcp` SDK's `FastMCP` stdio transport, register the server with Claude Code via `.mcp.json` (resolving RESEARCH.md Open Question 3 — this repo currently has neither `.claude/settings.json` nor `.mcp.json`), and document the CORPUS-04 Phase-5 integration point. This is the only plan in the phase that touches the `mcp` package's actual API surface.

**MCP SDK version-check requirement (resolves RESEARCH.md Open Question 2):** The `mcp` SDK's `FastMCP` API in the code examples throughout 04.1-RESEARCH.md is explicitly flagged `[ASSUMED]` — training-era knowledge, unverified against the installed 2.1.1 release. Task 1 below requires running a live check of the installed package's actual API BEFORE writing any decorator code.

Purpose: Make the corpus queryable by an educator via Claude Code (CORPUS-03's core success criterion) and give Phase 5 a documented, stable integration point (CORPUS-04) without building the sync endpoint itself.
Output: A runnable stdio MCP server, discoverable by Claude Code, plus integration documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-RESEARCH.md
@services/corpus_mcp/tools.py
@services/corpus_mcp/index.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify installed mcp SDK API, then implement server.py</name>
  <files>services/corpus_mcp/server.py</files>
  <read_first>04.1-RESEARCH.md "IMPORTANT CAVEAT — MCP SDK API surface" and Pattern 2 (Tool functions as plain Python functions, MCP registration as a thin wrapper)</read_first>
  <action>
Before writing any FastMCP code, run `pip show mcp` to confirm the installed version, then run `python -c "import mcp; help(mcp.server.fastmcp.FastMCP)"` (or `python -c "from mcp.server.fastmcp import FastMCP; help(FastMCP)"` if the first import path fails) to inspect the actual installed API: confirm (a) the exact import path for `FastMCP`, (b) the decorator syntax for registering tools (e.g. `@mcp_app.tool()` vs another form), (c) the exact stdio run invocation (e.g. `mcp_app.run(transport="stdio")` or equivalent). Do not assume the training-era 1.x pattern from 04.1-RESEARCH.md's code examples is correct — use whatever the installed 2.1.1 package's own help() output or bundled examples show.
Using the verified API, implement `services/corpus_mcp/server.py`: build the index once at module load via `INDEX = build_index(Path("docs/corpus/wiki"))` (or the correct root per `services/corpus_mcp/index.py`'s signature), instantiate the `FastMCP` app (name it `"etutor-corpus"`), and register 5 thin decorated wrapper functions — `get_topic`, `list_sources`, `search_corpus`, `get_experiments`, `list_topics` — each calling straight through to the matching `services/corpus_mcp/tools.py` `*_impl` function with `INDEX` as the first argument, per Pattern 2 (keep decorated functions thin pass-throughs so the underlying logic stays independently testable). End the module with the verified stdio run invocation, guarded by `if __name__ == "__main__":`.
  </action>
  <verify>
    <automated>python -c "import services.corpus_mcp.server" && grep -c "def " services/corpus_mcp/server.py | awk '{exit ($1 < 5)}'</automated>
  </verify>
  <done>server.py imports without error, defines 5 tool wrapper functions, and the module was written only after confirming the installed mcp package's actual FastMCP API via pip show/help() — not assumed from research examples.</done>
</task>

<task type="auto">
  <name>Task 2: Register server in .mcp.json</name>
  <files>.mcp.json</files>
  <read_first>04.1-RESEARCH.md Common Pitfall 2 (.claude/settings.json vs .mcp.json)</read_first>
  <action>
Create `.mcp.json` at the repo root (resolving RESEARCH.md Open Question 3 — neither `.claude/settings.json` nor `.mcp.json` exists yet in this repo; `.mcp.json` is the current Claude Code convention for project-scoped MCP servers, per D-11's intent of local stdio registration even though D-11's original text named `.claude/settings.json`). Register the server under an `mcpServers` key: key name `"etutor-corpus"`, `command: "python"`, `args: ["-m", "services.corpus_mcp.server"]`, `cwd` left unset (relative to repo root, since `.mcp.json` at repo root is invoked from that root by Claude Code). Do not modify `.claude/settings.json` — it is a different, non-MCP config file and D-11's language was superseded terminology per RESEARCH.md.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('.mcp.json')); assert 'etutor-corpus' in d['mcpServers']"</automated>
  </verify>
  <done>.mcp.json exists at repo root with a valid mcpServers.etutor-corpus entry pointing at services.corpus_mcp.server.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 5 CORPUS-04 integration point</name>
  <files>docs/corpus/INTEGRATION.md</files>
  <read_first>04.1-CONTEXT.md D-15; api/sync.py (existing stub, if present, to confirm current shape)</read_first>
  <action>
Write `docs/corpus/INTEGRATION.md` documenting, for the Phase 5 implementer, exactly how `api/sync.py`'s `SYNC-03` content-package bundling should consume this corpus per D-15: either (a) shell out to the MCP server's tool functions directly by importing `services/corpus_mcp/tools.py`'s `*_impl` functions and a locally-built index (no stdio/subprocess needed — same-process Python import, since both are part of the same FastAPI deployment), or (b) treat `docs/corpus/wiki/topics/` and `docs/corpus/wiki/experiments/` as the two directories to glob directly for a given set of focus KCs. State explicitly that this phase does not implement SYNC-03 — it only guarantees the corpus structure Phase 5 will consume won't require a schema change, per D-15. Reference the 5 tool signatures from D-13 as the contract Phase 5 can rely on.
  </action>
  <verify>
    <automated>grep -q "SYNC-03" docs/corpus/INTEGRATION.md && grep -q "services/corpus_mcp/tools.py" docs/corpus/INTEGRATION.md</automated>
  </verify>
  <done>docs/corpus/INTEGRATION.md exists, references SYNC-03 and the concrete tools.py functions/corpus directories Phase 5 should use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code (local process) -> MCP server (local stdio subprocess) | Both processes run under the same local user account per D-11; no network exposure |
| Future: FastAPI (api/sync.py) -> corpus | Documented (this plan) but not implemented; when Phase 5 wires it, the boundary is server-internal (same process), not attacker-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-08 | Tampering | .mcp.json command/args pointing at an unintended script if repo path assumptions change | accept | Local dev-only config, human-reviewed before commit; not a runtime attack surface per D-11's local-only scope |
| T-04.1-SC | Tampering | mcp / python-frontmatter package installs (npm/pip supply chain) | mitigate | Both packages passed slopcheck with Approved disposition per 04.1-RESEARCH.md Package Legitimacy Audit — no [ASSUMED]/[SUS] packages introduced in this phase, no blocking checkpoint required |
</threat_model>

<verification>
`python -m services.corpus_mcp.server` starts without a traceback (Ctrl-C to stop, since stdio servers block waiting for a client). Claude Code, when restarted in this repo, lists `etutor-corpus` as an available MCP server (manual check — not automatable in this sandbox).
</verification>

<success_criteria>
The MCP server runs over stdio, is discoverable via .mcp.json, exposes all 5 D-13 tools backed by real migrated + pilot content, and Phase 5's integration path is documented without requiring a corpus schema change.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-05-SUMMARY.md` when done
</output>
