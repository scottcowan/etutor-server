---
phase: 04.1-knowledge-corpus-and-mcp-server
plan: 03
type: tdd
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - services/corpus_mcp/tools.py
  - services/corpus_mcp/validate_topic_ids.py
  - tests/services/test_corpus_mcp.py
  - tests/test_validate_topic_ids.py
autonomous: true
requirements: [CORPUS-03, CORPUS-04]

must_haves:
  truths:
    - "Each of the 5 locked MCP tool behaviors (get_topic, list_sources, search_corpus, get_experiments, list_topics) is callable as a plain Python function and returns well-formed results against the fixture corpus"
    - "A topic_id argument that isn't a real index key never causes a filesystem path to be constructed from it — lookups are always through the index dict"
    - "Running the validation script against a deliberately mismatched fixture filename reports it as an orphan/missing pair"
  artifacts:
    - path: "services/corpus_mcp/tools.py"
      provides: "5 tool-implementation functions (D-13), each importable and unit-testable without stdio"
      exports: ["get_topic_impl", "list_sources_impl", "search_corpus_impl_wrapper", "get_experiments_impl", "list_topics_impl"]
    - path: "services/corpus_mcp/validate_topic_ids.py"
      provides: "Standalone CLI script diffing docs/corpus/wiki/topics/*.md filenames against services.curriculum.CURRICULUM ids"
      exports: ["find_mismatches"]
  key_links:
    - from: "services/corpus_mcp/tools.py get_topic_impl"
      to: "index[\"topics\"] dict lookup"
      via: "dict.get(topic_id) — never Path(root) / f\"{topic_id}.md\""
      pattern: "index\\[.topics.\\]\\.get\\("
    - from: "services/corpus_mcp/validate_topic_ids.py"
      to: "services/curriculum.py CURRICULUM"
      via: "import and set-diff against Topic.id values"
      pattern: "from services\\.curriculum import"
---

<objective>
Implement the 5 locked MCP tool behaviors (D-13) as plain, independently-testable Python functions on top of the index/search primitives from 04.1-02, plus the topic-ID drift validation script that keeps `docs/corpus/wiki/topics/*.md` filenames honest against `services/curriculum.py`. No `mcp`/`FastMCP` decorator wiring in this plan — that is 04.1-04's job (Pattern 2: keep tool logic decoupled from transport).

Purpose: Deliver the queryable-corpus behavior CORPUS-03 requires, and the topic-ID integrity check CORPUS-04's downstream Phase 5 consumer depends on not silently breaking.
Output: `services/corpus_mcp/tools.py`, `services/corpus_mcp/validate_topic_ids.py`, passing tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-RESEARCH.md
@services/corpus_mcp/index.py
@services/corpus_mcp/search.py
@services/curriculum.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 tool-implementation functions</name>
  <files>services/corpus_mcp/tools.py, tests/services/test_corpus_mcp.py</files>
  <read_first>04.1-CONTEXT.md D-13 (exact tool signatures); 04.1-RESEARCH.md Security Domain (path traversal mitigation — index dict lookup, never Path concatenation)</read_first>
  <behavior>
    - Test 1: `get_topic_impl(index, "fixture_topic_one")` returns the full entry dict for that key; `get_topic_impl(index, "fixture_topic_one", level="100")` returns only the matching `## 100 —` H2 section content, not the whole page; `get_topic_impl(index, "does_not_exist")` returns `{"error": ...}` (never raises, never touches the filesystem).
    - Test 2: `list_sources_impl(index, topic_id="fixture_topic_one")` returns only source entries whose `metadata["topic_ids"]` contains `fixture_topic_one`; passing an unmatched topic_id returns `[]`; an optional `type` filter further narrows by `metadata["type"]`.
    - Test 3: `get_experiments_impl(index, topic_id="fixture_topic_one", min_age=None)` returns matching experiment entries; when `min_age` is provided, entries with `flagged_dangerous: true` are excluded whenever the requested `min_age` is below the entry's own `min_age` (D-06 hard gate), while non-dangerous entries are returned regardless (D-06 permissive gate) — assert both branches explicitly with two fixture experiment entries (add a second fixture experiment with `flagged_dangerous: true, min_age: 12` if the Task 1 fixture set from 04.1-02 doesn't already cover this branch).
    - Test 4: `list_topics_impl(index, subject=None)` returns all topic ids present in the fixture index's "topics" bucket; with `subject="SomeSubject"` filters by `metadata.get("subject")` equality.
    - Test 5: A deliberately malicious `topic_id` value (e.g. `"../../../etc/passwd"`) passed to `get_topic_impl` returns the not-found error dict, exactly as an ordinary missing key would — confirming lookups never build a `Path` from the argument.
  </behavior>
  <action>
Implement all 5 functions in `services/corpus_mcp/tools.py`, each taking the pre-built `index` dict (from `build_index()`) as an explicit first argument — never a module-level global — per D-13's locked signatures: `get_topic_impl(index, topic_id, level=None)`, `list_sources_impl(index, topic_id, type_=None)`, a thin re-export of `search_corpus_impl` from `services/corpus_mcp/search.py` (import it directly, do not duplicate logic), `get_experiments_impl(index, topic_id, min_age=None)`, `list_topics_impl(index, subject=None)`. All lookups go through `index[bucket].get(key)` — never construct a `Path` from a caller-supplied string (per the Security Domain note in 04.1-RESEARCH.md: this makes path traversal structurally impossible). For `get_topic_impl`'s `level` parameter, split `entry["content"]` on `^## ` line boundaries and return only the section whose heading starts with the requested level number (e.g. `"100"` matches `## 100 — Introduction`). Use `Optional[X]` typing throughout (Python 3.9 compat per CLAUDE.md).
  </action>
  <verify>
    <automated>pytest tests/services/test_corpus_mcp.py -k "tools" -x</automated>
  </verify>
  <done>All 5 behavior tests pass; grep confirms zero occurrences of `Path(` constructed from a function parameter inside tools.py (only from the pre-built index).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate_topic_ids.py drift check</name>
  <files>services/corpus_mcp/validate_topic_ids.py, tests/test_validate_topic_ids.py</files>
  <read_first>04.1-RESEARCH.md Don't Hand-Roll (topic ID drift) and Pitfall 3</read_first>
  <behavior>
    - Test 1: `find_mismatches(corpus_topics_dir, valid_ids)` given a directory containing a file named `not_a_real_id.md` and `valid_ids={"real_id"}` returns a result distinguishing "orphans" (corpus files with no matching curriculum id — includes `not_a_real_id`) from "missing" (curriculum ids with no corpus file — includes `real_id`).
    - Test 2: When every corpus filename stem matches a valid id exactly (case-sensitive, per snake_case convention), both "orphans" and "missing" are empty.
  </behavior>
  <action>
Implement `find_mismatches(corpus_topics_dir: Path, valid_ids: set) -> dict` returning `{"orphans": [...], "missing": [...]}` via straightforward set difference: `corpus_ids = {f.stem for f in corpus_topics_dir.glob("*.md") if f.stem != "index"}`; `orphans = sorted(corpus_ids - valid_ids)`; `missing = sorted(valid_ids - corpus_ids)`.
Add a `main()` CLI entry point that imports `CURRICULUM` from `services.curriculum`, builds `valid_ids = {t.id for t in CURRICULUM}`, calls `find_mismatches(Path("docs/corpus/wiki/topics"), valid_ids)`, prints orphan/missing counts and lists, and exits non-zero if `orphans` is non-empty (missing is expected and non-fatal given the pilot-subject scope — see 04.1-05's Scope Resolution). Guard the CLI block with `if __name__ == "__main__":` so `find_mismatches` remains independently importable for tests.
  </action>
  <verify>
    <automated>pytest tests/test_validate_topic_ids.py -x</automated>
  </verify>
  <done>Both behavior tests pass; running `python -m services.corpus_mcp.validate_topic_ids` against the real docs/corpus/wiki/topics/ (populated by 04.1-05) reports zero orphans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool caller (Claude Code / educator) -> tool arguments | `topic_id`, `query`, `subject` etc. are attacker-adjacent in the sense that a misconfigured or malicious caller could pass path-traversal-style strings; local-only process per D-11 so severity is low but the mitigation is free (structural, not a special-case check) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Tampering / Information Disclosure | get_topic_impl / get_experiments_impl / list_sources_impl argument handling | mitigate | All lookups go through `index[bucket].get(key)`, never `Path(root) / f"{arg}.md"` — verified by Task 1 Test 5 (traversal string returns not-found, same as any other miss) |
| T-04.1-06 | Repudiation | Topic-ID drift going unnoticed until a runtime `get_topic` miss | mitigate | `validate_topic_ids.py` provides an explicit, scriptable check; non-zero exit on orphans makes it CI/pre-commit-ready even though wiring it into CI is out of scope this phase |
</threat_model>

<verification>
`pytest tests/services/test_corpus_mcp.py tests/test_validate_topic_ids.py -x` passes.
</verification>

<success_criteria>
All 5 D-13 tool behaviors are implemented as testable functions with structurally-safe index-based lookups. The topic-ID drift validator correctly classifies orphans vs missing ids against a real curriculum.py import.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-03-SUMMARY.md` when done
</output>
