---
phase: 04.1-knowledge-corpus-and-mcp-server
plan: 05
subsystem: corpus-mcp-server
tags: [python, mcp-sdk, fastmcp-renamed, stdio-transport, phase-5-integration]

requires:
  - phase: 04.1-knowledge-corpus-and-mcp-server
    provides: "services/corpus_mcp/tools.py *_impl functions (04.1-03), services/corpus_mcp/index.py build_index() (04.1-02), 21 migrated topic pages (04.1-04)"
provides:
  - "services/corpus_mcp/server.py — MCPServer stdio server wiring all 5 D-13 tools"
  - ".mcp.json — project-scoped MCP server registration for Claude Code"
  - "docs/corpus/INTEGRATION.md — Phase 5 SYNC-03 integration contract"
affects: [phase-5-device-sync]

tech-stack:
  added: []
  patterns:
    - "mcp 2.1.1's FastMCP class was renamed to MCPServer (import path mcp.server.mcpserver.MCPServer) — verified live via pip show + help() before writing any decorator code, per Task 1's mandatory API-verification step"
    - "INDEX built once at module import time via build_index(Path(\"docs/corpus\")) — root is the parent of wiki/, matching the FIXTURE_ROOT convention in tests/services/test_corpus_mcp.py, not docs/corpus/wiki itself"
    - "Each @mcp_app.tool() decorator is a one-line pass-through to the matching tools.py *_impl function — no logic in server.py"

key-files:
  created:
    - services/corpus_mcp/server.py
    - .mcp.json
    - docs/corpus/INTEGRATION.md

key-decisions:
  - "RESEARCH.md's training-era FastMCP import path (mcp.server.fastmcp.FastMCP) does not exist in the installed mcp 2.1.1 release — confirmed by a live ModuleNotFoundError with an explicit migration message pointing to mcp.server.mcpserver.MCPServer. Used the verified class throughout server.py instead of the plan's suggested variable naming assumption."
  - "build_index() root argument is docs/corpus (contains wiki/ as a subdirectory), not docs/corpus/wiki directly — confirmed against the FIXTURE_ROOT convention already established in tests/services/test_corpus_mcp.py rather than the plan's illustrative Path(\"docs/corpus/wiki\") suggestion."

requirements-completed: [CORPUS-03, CORPUS-04]

duration: 25min
completed: 2026-09-04
---

# Phase 04.1 Plan 05: MCP Server Transport Wiring + Phase 5 Integration Docs Summary

**stdio MCP server (`services/corpus_mcp/server.py`) wiring all 5 D-13 tools behind the installed `mcp` 2.1.1 SDK's `MCPServer` class (not `FastMCP`, which was renamed and no longer exists in this release), registered with Claude Code via `.mcp.json`, plus a documented Phase 5 SYNC-03 integration contract that requires no corpus schema change.**

## Performance

- **Duration:** 25 min
- **Tasks:** 3 completed
- **Files modified:** 3 (all created)

## Accomplishments

- Ran `pip show mcp` (via the project's `.venv`, since the ambient shell has no `mcp` package installed) and confirmed version 2.1.1
- Attempted `from mcp.server.fastmcp import FastMCP` per RESEARCH.md's training-era example — it raised a `ModuleNotFoundError` with an explicit migration message: FastMCP was renamed to `MCPServer` in `mcp.server.mcpserver`. This directly resolves RESEARCH.md Open Question 2 (the API surface was indeed different from the `[ASSUMED]`-flagged examples).
- Inspected the real `MCPServer` API via `help()` and `inspect.signature()`: `@mcp_app.tool()` decorator (unchanged in spirit), `mcp_app.run(transport="stdio")` (default transport is already `"stdio"`, passed explicitly for clarity)
- `services/corpus_mcp/server.py` builds `INDEX` once at module import via `build_index(Path("docs/corpus"))`, instantiates `MCPServer(name="etutor-corpus")`, and registers 5 thin decorated wrappers (`get_topic`, `list_sources`, `search_corpus`, `get_experiments`, `list_topics`) that pass straight through to `tools.py`'s `*_impl` functions
- Verified end-to-end against real migrated content: `INDEX["topics"]` has 21 entries (the 04.1-04 pilot subject pages), and `await mcp_app.list_tools()` returns all 5 registered tool names
- `python -m services.corpus_mcp.server </dev/null` exits cleanly with exit code 0 and no traceback (expected stdio-transport behavior when stdin closes with no client attached)
- `.mcp.json` created at repo root (neither it nor `.claude/settings.json` existed before) registering `etutor-corpus` under `mcpServers`, resolving RESEARCH.md Open Question 3
- `docs/corpus/INTEGRATION.md` documents two supported Phase 5 SYNC-03 consumption paths (direct import of `tools.py` `*_impl` functions, or direct glob of `wiki/topics`/`wiki/experiments`), and states explicitly that D-06 age-gating carries through automatically via Path A

## Task Commits

1. **Task 1: server.py — verify API, then wire 5 tools**
   - `ff264a4` feat(04.1-05): wire 5 MCP tools behind MCPServer stdio transport
2. **Task 2: .mcp.json registration**
   - `e3eb63b` feat(04.1-05): register etutor-corpus MCP server in .mcp.json
3. **Task 3: Phase 5 integration docs**
   - `32dea95` docs(04.1-05): document Phase 5 SYNC-03 integration point (D-15, CORPUS-04)

## Files Created/Modified

- `services/corpus_mcp/server.py` - MCPServer stdio server, 5 thin tool wrappers, module-level INDEX
- `.mcp.json` - project-scoped MCP server registration (`etutor-corpus` -> `python -m services.corpus_mcp.server`)
- `docs/corpus/INTEGRATION.md` - Phase 5 SYNC-03 integration contract (D-15)

## Decisions Made

- Used `mcp.server.mcpserver.MCPServer` instead of the plan's illustrative `FastMCP`/`mcp_app` naming from RESEARCH.md's `[ASSUMED]`-flagged examples, because the live installed package (2.1.1) has genuinely renamed the class — confirmed via a real `ModuleNotFoundError` with a migration-guide pointer, not an assumption. Kept the variable name `mcp_app` from the plan (naming convention, unaffected by the class rename).
- Passed `Path("docs/corpus")` (not `Path("docs/corpus/wiki")`) to `build_index()`, matching the `FIXTURE_ROOT` convention already established in `tests/services/test_corpus_mcp.py` (`fixtures/corpus`, which contains a `wiki/` subdirectory) rather than the plan's illustrative alternative path. This is a direct application of the plan's own instruction to defer to "the correct root per `services/corpus_mcp/index.py`'s signature" when the two suggestions diverge.

## Deviations from Plan

None requiring Rule 4 — the two decisions above were both explicitly anticipated and pre-authorized by the plan's own wording ("Do not assume the training-era 1.x pattern... use whatever the installed 2.1.1 package's own help() output... show[s]"; "or the correct root per services/corpus_mcp/index.py's signature").

## Issues Encountered

- The ambient shell's Python 3.9 (`/usr/bin/python3`) has no `mcp` package installed at all (`pip` itself is not on PATH in that environment). Found and used the project's actual `.venv` (Python 3.14, `mcp==2.1.1`) at `/Users/scowan/Projects/scottcowan/etutor-server/.venv` — this is the correct environment per `requirements.txt`'s `mcp>=2.1.0` pin. All verification commands in this summary were run through that `.venv`'s interpreter.

## Next Phase Readiness

- Phase 5's SYNC-03 implementer has a concrete, tested contract in `docs/corpus/INTEGRATION.md`: import `tools.py`'s `*_impl` functions with a `build_index(Path("docs/corpus"))`-built index, or glob `docs/corpus/wiki/topics/` and `docs/corpus/wiki/experiments/` directly. No corpus schema change is required either way.
- `services/corpus_mcp/validate_topic_ids.py` (04.1-03) can now be run against the populated `docs/corpus/wiki/topics/` tree as content grows beyond the current 21-topic pilot subject.
- The MCP server is ready for a human to restart Claude Code in this repo and confirm `etutor-corpus` appears as an available MCP server (manual check, not automatable in this sandbox, per the plan's own `<verification>` note).

## Self-Check: PASSED

- FOUND: services/corpus_mcp/server.py
- FOUND: .mcp.json
- FOUND: docs/corpus/INTEGRATION.md
- FOUND commit: ff264a4
- FOUND commit: e3eb63b
- FOUND commit: 32dea95

---
*Phase: 04.1-knowledge-corpus-and-mcp-server*
*Completed: 2026-09-04*
