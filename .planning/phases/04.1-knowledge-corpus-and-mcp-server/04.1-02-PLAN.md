---
phase: 04.1-knowledge-corpus-and-mcp-server
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - services/corpus_mcp/__init__.py
  - services/corpus_mcp/index.py
  - services/corpus_mcp/search.py
  - tests/services/test_corpus_mcp.py
  - tests/fixtures/corpus/wiki/topics/fixture_topic_one.md
  - tests/fixtures/corpus/wiki/topics/fixture_topic_two.md
  - tests/fixtures/corpus/wiki/sources/fixture_source_one.md
  - tests/fixtures/corpus/wiki/experiments/fixture_experiment_one.md
autonomous: true
requirements: [CORPUS-03]

must_haves:
  truths:
    - "A synthetic fixture corpus can be indexed in memory without touching the real docs/corpus/ content"
    - "search_corpus keyword matching finds fixture entries by content and by tag/topic_id, and excludes non-matching entries"
  artifacts:
    - path: "services/corpus_mcp/index.py"
      provides: "build_index() — one-time filesystem scan producing the in-memory corpus dict (D-14)"
      exports: ["build_index"]
    - path: "services/corpus_mcp/search.py"
      provides: "search_corpus keyword search over the index (D-14, no embeddings)"
      exports: ["search_corpus_impl"]
    - path: "tests/fixtures/corpus/"
      provides: "Small synthetic corpus (2 topics, 1 source, 1 experiment) so tests don't depend on evolving real content"
  key_links:
    - from: "services/corpus_mcp/search.py"
      to: "services/corpus_mcp/index.py build_index() output"
      via: "function argument (index dict), not a global import-time side effect"
      pattern: "def search_corpus_impl\\(.*index"
---

<objective>
Build the two lowest-level, independently-testable pieces of the MCP server: the in-memory index builder (`build_index()`) and the keyword search implementation (`search_corpus_impl()`). Both are plain Python functions with no MCP/stdio dependency, following Architecture Pattern 2 from research (business logic decoupled from transport) — this plan does not touch `mcp`/`FastMCP` at all.

Purpose: Establish the data layer every later corpus_mcp plan builds on, test-first, against a synthetic fixture corpus rather than the real (evolving) `docs/corpus/` content.
Output: `services/corpus_mcp/index.py`, `services/corpus_mcp/search.py`, passing tests, `mcp` + `python-frontmatter` added to requirements.txt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-CONTEXT.md
@.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-RESEARCH.md
@requirements.txt
@tests/services/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixture corpus + build_index()</name>
  <files>tests/fixtures/corpus/wiki/topics/fixture_topic_one.md, tests/fixtures/corpus/wiki/topics/fixture_topic_two.md, tests/fixtures/corpus/wiki/sources/fixture_source_one.md, tests/fixtures/corpus/wiki/experiments/fixture_experiment_one.md, services/corpus_mcp/__init__.py, services/corpus_mcp/index.py, tests/services/test_corpus_mcp.py, requirements.txt</files>
  <read_first>04.1-RESEARCH.md Pattern 1 (In-memory index) and Anti-Patterns; 04.1-CONTEXT.md D-14</read_first>
  <behavior>
    - Test 1: `build_index(root)` pointed at the fixture directory returns a dict with keys "topics", "sources", "experiments", "people"; `index["topics"]` contains exactly 2 entries keyed by filename stem (`fixture_topic_one`, `fixture_topic_two`).
    - Test 2: Each entry in the returned index is a dict with `metadata` (the parsed YAML frontmatter dict), `content` (markdown body string), and `path` (Path object) keys.
    - Test 3: `build_index` silently skips `index.md` (the master TOC file) even if present in the fixture tree — it must never appear as an indexed entry under any bucket.
    - Test 4: An empty bucket directory (e.g. no `people/` fixtures) still appears in the returned dict as an empty `{}`, not a missing key — callers must never need `index.get("people", {})`.
  </behavior>
  <action>
Add `mcp>=2.1.0` and `python-frontmatter>=1.3.0` to `requirements.txt` (append after the existing `itsdangerous` line, keep alphabetical-ish grouping consistent with the rest of the file).
Write 4 small fixture markdown files under `tests/fixtures/corpus/wiki/{topics,sources,experiments}/` — each with a minimal but schema-valid frontmatter block: the two topic fixtures use the D-03 H2 level-band structure (a `## 100 — Introduction` section is enough, no need for all four bands) plus simple frontmatter (`topic_ids: [fixture_topic_one]` etc. matching the filename stem); the source fixture uses the D-10 schema; the experiment fixture uses the D-07 schema with `flagged_dangerous: false`. Do not create a `people/` fixture directory — Test 4 exercises the empty-bucket case deliberately.
Implement `build_index(root: Path) -> dict` in `services/corpus_mcp/index.py` using `python-frontmatter`'s `frontmatter.load(path)` (returns an object with `.metadata` and `.content`), per `Optional[X]` typing convention (no `X | None`). Buckets are fixed at `{"topics": {}, "sources": {}, "experiments": {}, "people": {}}` regardless of which subdirectories actually exist under `root/wiki/` (satisfies Test 4). Glob `root.rglob("*.md")`, skip any file named `index.md`, bucket by `md_file.parent.name`, key by `md_file.stem`.
Add `services/corpus_mcp/__init__.py` (empty, makes this a package).
  </action>
  <verify>
    <automated>pytest tests/services/test_corpus_mcp.py -k build_index -x</automated>
  </verify>
  <done>All 4 behavior tests pass; build_index() has zero dependency on the real docs/corpus/ tree (takes root as a parameter, never hardcodes the path).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: search_corpus_impl() keyword search</name>
  <files>services/corpus_mcp/search.py, tests/services/test_corpus_mcp.py</files>
  <read_first>04.1-RESEARCH.md Code Examples (Simple keyword search) and D-14 ("simple keyword matching... no vector embeddings")</read_first>
  <behavior>
    - Test 1: Given the fixture index from Task 1, `search_corpus_impl(index, query="fixture")` (a substring present in both fixture topic bodies) returns results from both the "topics" bucket entries, each result a dict containing at minimum `bucket`, `id`, `metadata` keys.
    - Test 2: `search_corpus_impl(index, query="nonexistent-token-xyz")` returns an empty list.
    - Test 3: `search_corpus_impl(index, query="fixture", subject="NoSuchSubject")` returns an empty list — the `subject` filter is applied even when the raw query would otherwise match (metadata-based filtering happens before the substring check).
    - Test 4: Query matching is case-insensitive — `search_corpus_impl(index, query="FIXTURE")` returns the same results as the lowercase query.
  </behavior>
  <action>
Implement `search_corpus_impl(index: dict, query: str, subject: Optional[str] = None, level: Optional[int] = None, type_: Optional[str] = None) -> list[dict]` in `services/corpus_mcp/search.py`, following the research Code Examples pattern: lowercase the query, iterate every bucket and entry, apply `subject`/`level`/`type_` metadata filters first (skip the entry entirely if any provided filter doesn't match `entry["metadata"]`), then build a haystack from `entry["content"]` plus any `metadata.get("tags", [])` and `metadata.get("topic_ids", [])` joined and lowercased, and include the entry in results only if `query_lower in haystack`. Each result dict includes `bucket`, `id` (the index key), and `metadata`.
No external library — plain Python string operations only, consistent with D-14's explicit rejection of `rapidfuzz` for v1.
  </action>
  <verify>
    <automated>pytest tests/services/test_corpus_mcp.py -k search_corpus -x</automated>
  </verify>
  <done>All 4 behavior tests pass; search_corpus_impl takes the index as an explicit argument (no module-level global), matching the key_link requirement for testability without a running MCP process.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem content -> in-memory index | `build_index` trusts local markdown/frontmatter files (authored by the project owner, not an untrusted remote actor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03 | Denial of Service | search_corpus_impl over a growing corpus | accept | Local single-process, hundreds of files max per D-14/research; no result-count cap needed at this scale, revisit if corpus grows materially |
| T-04.1-04 | Tampering | Malformed/missing frontmatter causing build_index to crash the whole server | mitigate | python-frontmatter tolerates missing frontmatter (returns empty metadata dict) rather than raising; Task 1 tests confirm entries are always well-formed dicts even for minimal fixtures |
</threat_model>

<verification>
`pytest tests/services/test_corpus_mcp.py -x` passes. `pip show mcp python-frontmatter` (after `pip install -r requirements.txt`) shows both packages installed at the versions specified.
</verification>

<success_criteria>
build_index() and search_corpus_impl() are pure, testable functions with no MCP/stdio coupling, fully covered by unit tests against a synthetic fixture corpus, and requirements.txt lists the two new dependencies.
</success_criteria>

<output>
Create `.planning/phases/04.1-knowledge-corpus-and-mcp-server/04.1-02-SUMMARY.md` when done
</output>
