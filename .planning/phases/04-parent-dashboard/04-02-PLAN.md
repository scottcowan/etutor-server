---
phase: 04-parent-dashboard
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - db/crud.py
  - api/sessions.py
  - tests/db/test_crud_parent.py
autonomous: true
requirements: [PARENT-04]

must_haves:
  truths:
    - "end_session() creates an AlertModel row with alert_type='frustrated' when hint_used=True count > 3 for the same kc_id in a single session (D-10)"
    - "log_turn() sets safety_flag=True on an InteractionEventModel row when the question text contains a keyword from the safety keyword list (D-12)"
    - "get_alerts_for_child() returns only alerts from the last 30 days, ordered most-recent-first (D-14)"
    - "create_alert() persists an AlertModel row and returns it"
    - "update_child_profile() updates only the supplied fields; omitted fields remain unchanged (D-15, D-16)"
    - "get_all_mastery_for_child() returns a dict keyed by kc_id containing all MasteryStateModel rows for the child"
    - "extract_and_update_interests() new-interest alert is written when a new interest tag is added to the profile"
  artifacts:
    - path: "db/crud.py"
      exports: [get_all_mastery_for_child, get_alerts_for_child, create_alert, update_child_profile]
    - path: "api/sessions.py"
      provides: "end_session() frustration alert tally (D-10); log_turn safety_flag keyword check (D-12)"
    - path: "tests/db/test_crud_parent.py"
      provides: "TDD tests: update_child_profile, create_alert, get_alerts_for_child, frustration tally, safety_flag"
  key_links:
    - from: "api/sessions.py:end_session"
      to: "db/crud.py:create_alert"
      via: "func.count() GROUP BY kc_id WHERE hint_used=True; alert written when count > 3"
      pattern: "create_alert.*frustrated"
    - from: "api/chat.py OR api/sessions.py:log_turn path"
      to: "InteractionEventModel.safety_flag"
      via: "_has_safety_flag() keyword check on question text at log_turn time"
      pattern: "safety_flag"
    - from: "services/session_intelligence.py:extract_and_update_interests"
      to: "db/crud.py:create_alert"
      via: "if new_interest detected, write alert_type='new-interest'"
      pattern: "new-interest"
---

<objective>
Implement the alert data layer: add four CRUD functions to db/crud.py (`get_all_mastery_for_child`,
`get_alerts_for_child`, `create_alert`, `update_child_profile`), wire the D-10 frustration tally
into `end_session()`, add the D-12 safety_flag keyword check at `log_turn()` time, and wire the
D-11 new-interest alert into `extract_and_update_interests()`.

Purpose: The dashboard views in 04-03 will call these CRUD functions to render the alert feed,
mastery map, and profile editor. The tally and keyword check are the detection-side of PARENT-04.
Because all four CRUD functions have well-defined I/O (input → DB row(s)), TDD is the correct
approach.

Output: All tests in `tests/db/test_crud_parent.py` pass through RED → GREEN. Frustration alerts
written at session end; safety_flag set at log_turn time; new-interest alerts written by
extract_and_update_interests.
</objective>

<execution_context>
@/Users/scowan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scowan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-parent-dashboard/04-CONTEXT.md
@.planning/phases/04-parent-dashboard/04-RESEARCH.md
@.planning/phases/04-parent-dashboard/04-PATTERNS.md
@.planning/phases/03-session-intelligence/03-04-SUMMARY.md
</context>

<feature>
  <name>Alert data layer — CRUD functions + session wiring</name>
  <files>db/crud.py, api/sessions.py, tests/db/test_crud_parent.py</files>
  <behavior>
    CRUD:
    - update_child_profile(child_id, session, *, name=None, age=None, reading_level=None, neurodivergence=None, interests=None) → Optional[ChildProfileModel]: updates only non-None fields; returns updated child; returns None if child_id not found
    - create_alert(child_id, alert_type, session, *, snippet=None, kc_id=None, session_id=None) → AlertModel: persists new AlertModel row with uuid4 id and triggered_at=datetime.now(timezone.utc)
    - get_alerts_for_child(child_id, session, days=30) → list[AlertModel]: returns alerts where triggered_at >= now - 30d, ordered triggered_at DESC
    - get_all_mastery_for_child(child_id, session) → dict[str, MasteryStateModel]: returns {kc_id: row} for all MasteryStateModel rows matching child_id

    Session wiring:
    - end_session() frustration tally (D-10): after existing BKT updates, query `func.count(InteractionEventModel.id)` GROUP BY kc_id WHERE session_id=X AND hint_used=True AND kc_id IS NOT NULL; for each row where hint_count > 3, call create_alert(child_id, "frustrated", kc_id=row.kc_id, session_id=session_id)
    - log_turn / safety_flag (D-12): define `_SAFETY_KEYWORDS: frozenset[str]` module-level constant in services/sessions.py (or db/crud.py — wherever log_turn is implemented); keywords include: "suicide", "self-harm", "kill myself", "die", "hurt myself", "abuse", "naked", "sex", "drugs", "weapons", "bomb", "hurt someone", "run away", "dangerous"; define `_has_safety_flag(text: str) -> bool`; when log_turn is called, pass `safety_flag=_has_safety_flag(question)` (or equivalent) to `InteractionEventModel(safety_flag=...)`. NOTE: read db/crud.py and api/chat.py to determine where log_turn lives and which parameter carries the question text.
    - extract_and_update_interests new-interest alert (D-11): in services/session_intelligence.py, after calling `update_interests()`, compare the returned interests list against the pre-update list; for each new tag, call `create_alert(child_id, "new-interest", snippet=new_tag)`. NOTE: read the current signature of extract_and_update_interests to determine how to pass the db session and child_id — these may already be parameters.

    Test cases (TDD — write tests FIRST, verify RED, then implement GREEN):
    - test_update_child_profile_name: create child, update name → new name in DB
    - test_update_child_profile_partial: update only reading_level → other fields unchanged
    - test_update_child_profile_not_found: unknown child_id → returns None
    - test_create_alert_persists: create_alert with alert_type="frustrated" → row in DB with correct fields
    - test_get_alerts_for_child_returns_30_day_window: insert alert at now-31d → excluded; alert at now-1d → included
    - test_get_alerts_ordered_desc: insert two alerts → most recent first
    - test_get_all_mastery_for_child_returns_dict: insert 2 MasteryStateModel rows → dict keyed by kc_id
    - test_frustration_alert_created_at_end_session: 4 InteractionEventModel rows with hint_used=True on same kc_id → AlertModel row with alert_type="frustrated" exists after tally runs
    - test_safety_flag_set_on_keyword_match: log_turn with question containing "suicide" → InteractionEventModel.safety_flag = True
    - test_safety_flag_not_set_on_clean_text: log_turn with "what is photosynthesis" → safety_flag is None or False
  </behavior>
  <implementation>
    RED phase:
    Create tests/db/test_crud_parent.py with all 10 test stubs. Use the db_session fixture from conftest.py (not a new mem_engine — check tests/conftest.py for the shared fixture). Import the four CRUD functions that do not exist yet → tests will fail with ImportError/AttributeError at collection time.
    Run: `pytest tests/db/test_crud_parent.py -x -q` → confirm ALL fail (ImportError or AttributeError is acceptable for RED).

    GREEN phase:
    1. db/crud.py: add `update_child_profile`, `create_alert`, `get_alerts_for_child`, `get_all_mastery_for_child` following the exact patterns in 04-PATTERNS.md lines 263-342. Add `import uuid` if not already present. Add `from db.models import AlertModel` to the imports.

    2. api/sessions.py (end_session): read the current end_session implementation fully before editing. Add the frustration tally block after the existing BKT/interest-extraction calls. Import `func` from sqlalchemy (may already be imported — check). Import `create_alert` from db.crud. The tally query uses `InteractionEventModel` and `func.count()` GROUP BY kc_id (per 04-PATTERNS.md lines 344-367).

    3. safety_flag keyword check: read db/crud.py to find the `log_turn` (or `create_interaction_event`) function. Read api/chat.py to see how the question text flows into log_turn. Add `_SAFETY_KEYWORDS` frozenset and `_has_safety_flag()` function in the same module as log_turn. Pass `safety_flag=_has_safety_flag(question_text)` to the InteractionEventModel constructor. The `safety_flag` column already exists after 04-01 migration.

    4. services/session_intelligence.py (new-interest alert): read `extract_and_update_interests` signature fully. After `update_interests()` returns, compare the new interests list against the pre-update interests. For each newly added interest, call `create_alert(child_id, "new-interest", snippet=new_tag, session=db_session)`. This requires the db session to be available in extract_and_update_interests — it likely already is (it calls update_interests which needs a session); confirm by reading the function.

    Run: `pytest tests/db/test_crud_parent.py -x -q` → ALL 10 pass.
    Run: `pytest tests/ -x -q --ignore=tests/evals` → full suite still green.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child question text→safety_flag check | Untrusted child speech (via Whisper STT) evaluated against keyword list |
| end_session POST→frustration tally | Session ID from device; must belong to a real session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02-01 | Information Disclosure | AlertModel child_id FK | mitigate | Alert rows are gated by authenticated parent cookie at the view layer (04-03); `get_alerts_for_child` requires explicit child_id — no endpoint exposes all alerts |
| T-4-02-02 | Tampering | safety_flag keyword list | accept | Keyword list is a compile-time frozenset constant — not configurable at runtime; low attack surface; LLM-based safety is Phase 6 SAFETY-01 |
| T-4-02-03 | Information Disclosure | Whisper STT text written to InteractionEventModel | accept | COPPA: no raw audio stored; question text is stored per DB-04 requirement; access gated by parent auth |
| T-4-02-04 | Denial of Service | 870-row mastery query per page load | accept | Single `SELECT WHERE child_id = X` uses composite PK index; O(870) rows at v1 scale; no optimisation needed |
| T-4-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — itsdangerous already handled in 04-01 |
</threat_model>

<verification>
TDD gate sequence:

RED: `pytest tests/db/test_crud_parent.py -x -q` → all 10 tests fail (ImportError / AttributeError)

GREEN: `pytest tests/db/test_crud_parent.py -x -q` → all 10 tests pass

Full suite: `pytest tests/ -x -q --ignore=tests/evals` → no regressions

Spot checks:
```bash
grep -c 'create_alert' db/crud.py | grep -v '^0$' && echo PASS || echo FAIL
grep -c '_SAFETY_KEYWORDS' db/crud.py services/sessions.py 2>/dev/null | grep -v ':0' | head -1
grep -c 'hint_count' api/sessions.py | grep -v '^0$' && echo PASS || echo FAIL
```
</verification>

<success_criteria>
1. All 10 tests in tests/db/test_crud_parent.py pass (RED confirmed before GREEN)
2. Frustration alert row (alert_type='frustrated') created when 4 hint_used=True events on same kc_id in one session
3. safety_flag=True set on InteractionEventModel row when question contains a keyword
4. Full test suite green with no regressions (pytest tests/ -x --ignore=tests/evals)
5. update_child_profile returns None for unknown child_id (no unhandled exception)
</success_criteria>

<output>
Create `.planning/phases/04-parent-dashboard/04-02-SUMMARY.md` when done.
</output>
