---
phase: 04-parent-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/settings.py
  - api/main.py
  - api/parent.py
  - web/parent/templates/login.html
  - db/models.py
  - migrations/versions/XXXX_phase4_parent.py
  - tests/api/test_parent_auth.py
autonomous: true
requirements: [PARENT-05]

must_haves:
  truths:
    - "GET /parent without a valid session cookie redirects to /parent/login (per D-05)"
    - "POST /parent/login with correct passphrase (D-01) sets httpOnly session cookie and redirects to /parent"
    - "POST /parent/login with wrong passphrase redirects to /parent/login with no error message (per D-04)"
    - "POST /parent/logout clears session and redirects to /parent/login"
    - "api/chat.py re-reads child profile from DB per request — no in-memory caching (D-17 verified, no code change needed)"
    - "alerts table and safety_flag column exist in schema after alembic upgrade head"
  artifacts:
    - path: "requirements.txt"
      contains: "itsdangerous"
    - path: "config/settings.py"
      contains: "parent_password"
    - path: "api/main.py"
      contains: "SessionMiddleware"
    - path: "api/parent.py"
      exports: [require_parent_auth, login_page, do_login, do_logout]
    - path: "web/parent/templates/login.html"
      provides: "Single-field passphrase login form (D-03)"
    - path: "db/models.py"
      contains: ["safety_flag", "AlertModel"]
    - path: "tests/api/test_parent_auth.py"
      provides: "Auth tests: redirect, cookie set, wrong passphrase"
  key_links:
    - from: "api/main.py"
      to: "starlette.middleware.sessions.SessionMiddleware"
      via: "app.add_middleware — must be added AFTER CORSMiddleware (reverse execution order)"
      pattern: "SessionMiddleware"
    - from: "api/parent.py:require_parent_auth"
      to: "request.session['parent_authenticated']"
      via: "FastAPI Depends(require_parent_auth) on all /parent/* routes except login/logout"
    - from: "api/parent.py:do_login"
      to: "settings.parent_password"
      via: "hmac.compare_digest (constant-time; per D-01)"
---

<objective>
Install the `itsdangerous` dependency that `SessionMiddleware` requires, configure the parent
passphrase, wire session middleware into FastAPI, add auth routes and login template, add the
`safety_flag` column and `AlertModel` table to the ORM and run the Alembic migration.

Purpose: All subsequent Phase 4 plans depend on the session cookie infrastructure and DB schema
changes built here. The `require_parent_auth` dependency created in this plan gates every protected
dashboard route (per D-05, fixes CR-02 for HTML views).

Output: Working passphrase login/logout flow at `/parent/login` and `/parent/logout`; `alerts` table
and `safety_flag` column present in the DB; auth tests green.
</objective>

<execution_context>
@/Users/scowan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scowan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-parent-dashboard/04-CONTEXT.md
@.planning/phases/04-parent-dashboard/04-RESEARCH.md
@.planning/phases/04-parent-dashboard/04-PATTERNS.md
@.planning/phases/03-session-intelligence/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: itsdangerous dep, parent_password config, SessionMiddleware</name>
  <files>requirements.txt, config/settings.py, api/main.py</files>
  <read_first>
    - requirements.txt — verify itsdangerous is absent; note existing version-pinning style (>=)
    - config/settings.py — find the Settings class; locate secret_key field (line ~8); follow the BaseSettings field pattern for adding parent_password
    - api/main.py — find the CORSMiddleware block (lines ~38-43); SessionMiddleware must be added AFTER it (Starlette executes middleware in reverse-addition order); locate get_settings() call or module-level settings object
  </read_first>
  <action>
    1. requirements.txt: append `itsdangerous>=2.1.2` on a new line following the existing `>=` version-pinning style. Run `pip install itsdangerous` (or confirm already installed after requirements.txt edit).

    2. config/settings.py: add `parent_password: str = "change-me-in-env"` as a new field on the `Settings` class, immediately after the `secret_key` field. This field is loaded from the `PARENT_PASSWORD` env var via pydantic-settings (the existing `Config.env_file = "config/.env"` already handles this). Do NOT add a comment exposing the default value to logs.

    3. api/main.py: import `SessionMiddleware` from `starlette.middleware.sessions`. After the existing `app.add_middleware(CORSMiddleware, ...)` block, add:
       `app.add_middleware(SessionMiddleware, secret_key=get_settings().secret_key, https_only=False)`
       Use the existing `get_settings()` call pattern already present in the file (do not introduce a new module-level `settings` variable if one does not already exist — check first). `https_only=False` is correct for local dev HTTP; production will sit behind TLS.

    D-17 VERIFICATION (no code change): confirm that `api/chat.py` calls `get_child_by_id(child_id, session)` inside the request handler using the `Depends(get_db)` session — it does per 04-PATTERNS.md line 157. Record this as a confirmed truth in the SUMMARY; no edit required.
  </action>
  <verify>
    <automated>python -c "from starlette.middleware.sessions import SessionMiddleware; print('itsdangerous OK')"</automated>
    <automated>grep -v '^#' requirements.txt | grep -c 'itsdangerous' | grep -q '^[1-9]' && echo 'PASS' || echo 'FAIL'</automated>
    <automated>python -c "from config.settings import get_settings; s = get_settings(); get_settings.cache_clear(); assert hasattr(s, 'parent_password'), 'parent_password missing'"</automated>
    <automated>python -c "from api.main import app; mw = [str(type(m)) for m in app.middleware_stack.__dict__.get('app', app).__class__.__mro__]; print('main imports OK')"</automated>
  </verify>
  <done>
    `itsdangerous>=2.1.2` in requirements.txt; `parent_password` field on Settings; `SessionMiddleware` wired in api/main.py with correct order; import of SessionMiddleware succeeds without ModuleNotFoundError.
  </done>
</task>

<task type="auto">
  <name>Task 2: Auth routes, require_parent_auth dep, login template, auth tests</name>
  <files>api/parent.py, web/parent/templates/login.html, tests/api/test_parent_auth.py</files>
  <read_first>
    - api/parent.py — read the entire file; note the existing router, Jinja2Templates wiring, and any existing routes; find the import block (follow api/child.py pattern per 04-PATTERNS.md lines 38-50)
    - web/parent/templates/dashboard.html — extract the CSS variables block (lines 7-45 approx): `font-family`, `background: #f8f8f8`, card styles (`background: white; border: 1px solid #e0e0e0; border-radius: 12px`), green accent `#7a9e7e`; login.html must inherit this aesthetic
    - tests/api/test_session_end.py — read fixture structure (lines 29-83): mem_engine, async_sessionmaker, dependency_overrides pattern; use as template for test_parent_auth.py
    - 04-UI-SPEC.md — Login Page section: max-width 360px card, password field with autocomplete="current-password", "Passphrase" label, "Sign in" button, lang="en"
  </read_first>
  <action>
    In api/parent.py — add the following, keeping all existing code intact:

    IMPORTS: add `import hmac` and `from fastapi import Depends, Request` (may already exist), `from fastapi.responses import HTMLResponse, RedirectResponse`, `from config.settings import get_settings`.

    DEPENDENCY: define `require_parent_auth(request: Request)` — if `not request.session.get("parent_authenticated")`, raise `RedirectResponse(url="/parent/login", status_code=303)`. This is a plain function (not async) — Starlette reads `request.session` synchronously. Per D-05, this dep gates all routes except login and logout.

    ROUTES:
    - `GET /login` (response_class=HTMLResponse): render `login.html` with `{"request": request}`. No auth dep on this route.
    - `POST /login` (response_class=RedirectResponse): read `form.get("password", "")` from `await request.form()`; call `get_settings()` (not cached var — the function is already lru_cached); use `hmac.compare_digest(password, settings.parent_password)` (per D-01/RESEARCH Pattern 2). On match: `request.session["parent_authenticated"] = True`, return `RedirectResponse("/parent", 303)`. On mismatch: return `RedirectResponse("/parent/login", 303)` — no error message (D-04).
    - `POST /logout` (response_class=RedirectResponse): call `request.session.clear()`, return `RedirectResponse("/parent/login", 303)`.

    All three new routes use `router` (existing). Do NOT add `_: None = Depends(require_parent_auth)` to the login/logout routes.

    Create web/parent/templates/login.html: minimal HTML page matching D-03 and 04-UI-SPEC Login Page spec. Key elements: `lang="en"`, `<title>Parent Dashboard</title>`, `<body style="background:#f8f8f8; font-family: -apple-system, sans-serif">`, centred card `max-width: 360px; padding: 32px; background: white; border: 1px solid #e0e0e0; border-radius: 12px; margin: 80px auto`, heading "Parent Dashboard" at 20px/600, `<form method="POST" action="/parent/login">`, `<label>Passphrase</label>`, `<input type="password" name="password" autocomplete="current-password" style="width:100%;...">`, `<button type="submit" style="background:#7a9e7e;color:white;border:none;border-radius:8px;padding:8px 24px;font-size:16px;font-weight:600;cursor:pointer;width:100%;">Sign in</button>`. No error message shown regardless of query params.

    Create tests/api/test_parent_auth.py: use the mem_engine + test_client fixture pattern from test_session_end.py. Override `PARENT_PASSWORD` by calling `get_settings.cache_clear()` in fixture teardown and setting `os.environ["PARENT_PASSWORD"] = "test-password"` before constructing the client. Write these tests:
    - `test_protected_route_without_cookie_redirects`: GET /parent, follow_redirects=False → status 302/303, location contains "/parent/login"
    - `test_login_page_returns_200`: GET /parent/login → status 200
    - `test_login_sets_cookie_and_redirects`: POST /parent/login with data={"password": "test-password"}, follow_redirects=False → status 302/303 and "session" in response.cookies
    - `test_wrong_passphrase_redirects_no_error`: POST /parent/login with data={"password": "wrong"}, follow_redirects=False → status 302/303, location contains "/parent/login"
    - `test_logout_clears_session`: login then POST /parent/logout → status 302/303, location contains "/parent/login"
    - `test_authenticated_get_parent_returns_200`: login then GET /parent → status 200 (NOTE: this test will fail until 04-03 adds the dashboard route; mark with `pytest.mark.skip(reason="dashboard route added in 04-03")` if needed, OR verify the existing GET / route in api/parent.py works with the new auth dep — check what the existing route returns and adapt accordingly)
  </action>
  <verify>
    <automated>pytest tests/api/test_parent_auth.py -x -q</automated>
  </verify>
  <done>
    Five auth tests pass (login/logout/redirect/cookie); login.html renders a single-field form matching D-03; `require_parent_auth` dep defined and importable from api/parent.py.
  </done>
</task>

<task type="auto">
  <name>Task 3: AlertModel + safety_flag migration</name>
  <files>db/models.py, migrations/versions/XXXX_phase4_parent.py</files>
  <read_first>
    - db/models.py — read the entire file; find InteractionEventModel (lines ~52-71): note the column declaration pattern (`Mapped[Optional[bool]]`, `mapped_column(Boolean, nullable=True)`); find MasteryStateModel for the FK pattern; find the last line of the file to know where to append AlertModel
    - migrations/versions/d8909e3e5f75_add_child_fsrs_params.py — read for the migration file structure: `revision`, `down_revision` (set this to 'd8909e3e5f75'), `branch_labels`, `depends_on`, `upgrade()`, `downgrade()` functions; column addition via `op.add_column`; table creation via `op.create_table`
  </read_first>
  <action>
    In db/models.py:

    1. Add `safety_flag: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)` to `InteractionEventModel`, appended after the existing `hint_used` column line. Follow the exact `Mapped[Optional[bool]]` typing pattern (Python 3.9 compat — not `bool | None`).

    2. Add `AlertModel` class at the end of the file (before or after the final `__all__` if present). Required imports: `ForeignKey` is already imported; ensure `AlertModel` follows the exact pattern from 04-PATTERNS.md lines 210-227:
       - `__tablename__ = "alerts"`
       - `id: Mapped[str]` — String primary key (uuid4 assigned at creation time by CRUD, not default here)
       - `child_id: Mapped[str]` — String FK to child_profiles.id, nullable=False, index=True
       - `alert_type: Mapped[str]` — String, nullable=False (values: 'frustrated', 'sensitive', 'new-interest')
       - `triggered_at: Mapped[datetime]` — DateTime(timezone=True), default=`lambda: datetime.now(timezone.utc)` (never `datetime.utcnow()`)
       - `snippet: Mapped[Optional[str]]` — String, nullable=True (question text excerpt, max 80 chars at write time)
       - `kc_id: Mapped[Optional[str]]` — String, nullable=True (for 'frustrated' alerts)
       - `session_id: Mapped[Optional[str]]` — String, FK to sessions.id, nullable=True

    Generate the Alembic migration: run `alembic revision --autogenerate -m "phase4_parent_safety_flag_and_alerts"`. The autogenerated file will be in `migrations/versions/`. Verify it contains:
    - `op.add_column('interaction_events', sa.Column('safety_flag', sa.Boolean(), nullable=True))`
    - `op.create_table('alerts', ...)` with all AlertModel columns
    - `op.create_index(op.f('ix_alerts_child_id'), 'alerts', ['child_id'], unique=False)`
    - `down_revision` set to `'d8909e3e5f75'`

    If autogenerate omits any column, add it manually following migrations/versions/d8909e3e5f75_add_child_fsrs_params.py pattern. Then run `alembic upgrade head` to apply.
  </action>
  <verify>
    <automated>alembic upgrade head</automated>
    <automated>alembic downgrade -1 && alembic upgrade head</automated>
    <automated>python -c "from db.models import AlertModel, InteractionEventModel; print('safety_flag:', hasattr(InteractionEventModel, 'safety_flag')); print('AlertModel OK')"</automated>
    <automated>pytest tests/ -x -q --ignore=tests/evals</automated>
  </verify>
  <done>
    `alembic upgrade head` runs cleanly on a fresh DB; `alembic downgrade -1 && alembic upgrade head` round-trips without error; `AlertModel` and `safety_flag` importable from `db.models`; full test suite still green (no regressions from model changes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→POST /parent/login | Untrusted passphrase string from HTTP form body |
| session cookie→all /parent/* | httpOnly signed cookie; forgeable only if secret_key leaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01-01 | Spoofing | POST /parent/login passphrase compare | mitigate | `hmac.compare_digest(password, settings.parent_password)` — stdlib constant-time comparison; prevents timing side-channel brute-force |
| T-4-01-02 | Information Disclosure | session cookie in transit | mitigate | `SessionMiddleware(https_only=False)` for dev; production must set `https_only=True` — document in SUMMARY; httpOnly flag (set by SessionMiddleware by default) prevents XSS theft |
| T-4-01-03 | Information Disclosure | PARENT_PASSWORD in source | mitigate | Field loaded from `PARENT_PASSWORD` env var via pydantic-settings; default "change-me-in-env" in code is non-functional; config/.env is git-ignored |
| T-4-01-04 | Elevation of Privilege | require_parent_auth bypass via missing dep | mitigate | `require_parent_auth` is a FastAPI `Depends` — router param `_: None = Depends(require_parent_auth)` cannot be omitted silently; verify each new /parent/* route in 04-03 includes the dep |
| T-4-01-SC | Tampering | itsdangerous install supply chain | accept | Pallets project (same org as Flask/Jinja2); Package Legitimacy Audit in 04-RESEARCH.md approves; no slopcheck flag |
</threat_model>

<verification>
After all three tasks:

```bash
# Auth flow round-trip
pytest tests/api/test_parent_auth.py -x -q

# Migration round-trip
alembic downgrade -1 && alembic upgrade head

# Model imports
python -c "from db.models import AlertModel, InteractionEventModel; assert hasattr(InteractionEventModel, 'safety_flag')"

# SessionMiddleware wired
python -c "from api.main import app; print([type(m).__name__ for m in app.user_middleware])"

# Full suite (no regressions)
pytest tests/ -x -q --ignore=tests/evals
```
</verification>

<success_criteria>
1. `from starlette.middleware.sessions import SessionMiddleware` imports without ModuleNotFoundError
2. GET /parent without cookie → 302/303 to /parent/login
3. POST /parent/login with correct passphrase → 302/303 to /parent, "session" cookie set
4. POST /parent/login with wrong passphrase → 302/303 to /parent/login, no "session" cookie
5. `alembic upgrade head` runs cleanly; `alembic downgrade -1` reverts cleanly
6. `AlertModel` and `InteractionEventModel.safety_flag` importable from `db.models`
7. All existing tests still pass (pytest tests/ -x --ignore=tests/evals)
</success_criteria>

<output>
Create `.planning/phases/04-parent-dashboard/04-01-SUMMARY.md` when done.
</output>
