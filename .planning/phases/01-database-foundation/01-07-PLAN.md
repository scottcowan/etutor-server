---
phase: 01-database-foundation
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - api/sessions.py
  - api/sync.py
  - api/child.py
  - api/parent.py
  - api/dashboard.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03

must_haves:
  truths:
    - "api/sessions.py GET /v1/sessions/{child_id} passes session to get_session_history and serialises ORM objects without vars()"
    - "api/sync.py GET /v1/devices/{device_id}/sync passes session to get_child_by_device_id"
    - "api/child.py passes session to list_children and get_child_by_id"
    - "api/parent.py passes session to list_children and get_all_sessions"
    - "api/dashboard.py passes session to list_children and get_all_sessions; serialises ORM objects without vars()"
    - "All 5 route files import cleanly with no TypeError or ImportError"
    - "pytest tests/db/ -q still all green after these changes"
  artifacts:
    - path: "api/sessions.py"
      provides: "Session history route with DB session injection"
      contains: "Depends(get_db)"
    - path: "api/sync.py"
      provides: "Device sync route with DB session injection"
      contains: "Depends(get_db)"
    - path: "api/child.py"
      provides: "Child UI routes with DB session injection"
      contains: "Depends(get_db)"
    - path: "api/parent.py"
      provides: "Parent UI routes with DB session injection"
      contains: "Depends(get_db)"
    - path: "api/dashboard.py"
      provides: "Dashboard JSON route with DB session injection"
      contains: "Depends(get_db)"
  key_links:
    - from: "api/sessions.py"
      to: "services/sessions.get_session_history"
      via: "session injected by Depends(get_db)"
      pattern: "get_session_history"
    - from: "api/dashboard.py"
      to: "services/sessions.get_all_sessions"
      via: "session injected; ORM objects serialised explicitly"
      pattern: "get_all_sessions"
---

<objective>
Wire Depends(get_db) session injection into the remaining five API route files and replace all `vars(t)` ORM serialisation with explicit dict construction. After this plan, every route that calls a service function passes an AsyncSession.

Purpose: Completes the full session injection thread. Plans 05 and 06 left these five routes calling service functions without a session — they would raise TypeError at runtime. Running in parallel with Plan 06 (disjoint files).

Output: Five modified route files. All imports clean. All tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
@.planning/phases/01-database-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject session into api/sessions.py and api/sync.py</name>
  <files>api/sessions.py, api/sync.py</files>
  <read_first>
    - api/sessions.py — read in full; note vars(t) usage at line 10 — must be replaced
    - api/sync.py — read in full; get_child_by_device_id called without session at line 10
    - services/sessions.py (migrated in Plan 05) — confirm get_session_history signature: (child_id, session, limit=50)
    - services/profiles.py (migrated in Plan 05) — confirm get_child_by_device_id signature: (device_id, session)
    - .planning/phases/01-database-foundation/01-PATTERNS.md — sections "api/sessions.py *(modify)*" and "api/sync.py *(modify)*"
  </read_first>
  <action>
    Edit api/sessions.py:

    Add imports:
      from fastapi import Depends
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.session import get_db

    Change route signature from:
      async def get_sessions(child_id: str, limit: int = 50):
    to:
      async def get_sessions(child_id: str, limit: int = 50, session: AsyncSession = Depends(get_db)):

    Change service call from:
      turns = await get_session_history(child_id, limit)
    to:
      turns = await get_session_history(child_id, session, limit)

    Replace the vars(t) serialisation. The current return:
      return {"child_id": child_id, "turns": [vars(t) for t in turns]}
    becomes:
      return {"child_id": child_id, "turns": [
          {
              "id": t.id,
              "child_id": t.child_id,
              "question": t.question,
              "answer": t.answer,
              "timestamp": t.timestamp.isoformat() if t.timestamp else None,
              "topic": t.topic,
              "session_id": t.session_id,
          }
          for t in turns
      ]}

    CRITICAL: vars() does not work on SQLAlchemy ORM objects — it includes _sa_instance_state which is not JSON-serialisable. Must serialise explicitly as shown above.

    Edit api/sync.py:

    Add imports:
      from fastapi import Depends
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.session import get_db

    Change route signature from:
      async def sync_device(device_id: str):
    to:
      async def sync_device(device_id: str, session: AsyncSession = Depends(get_db)):

    Change service call from:
      child = await get_child_by_device_id(device_id)
    to:
      child = await get_child_by_device_id(device_id, session)

    Do NOT change the return shape or the build_recommendation_message call — those are unchanged.
  </action>
  <verify>
    <automated>python -c "import api.sessions; import api.sync; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import api.sessions; import api.sync; print('OK')"` prints "OK"
    - grep -c "Depends(get_db)" api/sessions.py returns 1
    - grep -c "Depends(get_db)" api/sync.py returns 1
    - grep -c "vars(t)" api/sessions.py returns 0 (removed)
    - grep -c "t.timestamp.isoformat()" api/sessions.py returns 1 (explicit serialisation)
    - grep -c "get_child_by_device_id(device_id, session)" api/sync.py returns 1
  </acceptance_criteria>
  <done>api/sessions.py and api/sync.py inject session; vars() replaced with explicit serialisation in sessions.py.</done>
</task>

<task type="auto">
  <name>Task 2: Inject session into api/child.py, api/parent.py, api/dashboard.py</name>
  <files>api/child.py, api/parent.py, api/dashboard.py</files>
  <read_first>
    - api/child.py — read in full; list_children and get_child_by_id called without session
    - api/parent.py — read in full; list_children and get_all_sessions called without session
    - api/dashboard.py — read in full; vars(c) and vars(t) usage — must be replaced with explicit serialisation
    - services/profiles.py (migrated) — list_children(session) signature
    - services/sessions.py (migrated) — get_session_history(child_id, session, limit=50); note: get_all_sessions is not in the migrated services/sessions.py (it was in-memory only); must handle this
    - .planning/phases/01-database-foundation/01-PATTERNS.md — sections "api/child.py", "api/parent.py", "api/parent.py"
  </read_first>
  <action>
    Edit api/child.py:

    Add imports:
      from fastapi import Depends
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.session import get_db

    Add `session: AsyncSession = Depends(get_db)` parameter to both route handlers:
      child_home(request, session=Depends(get_db))
      child_chat(request, child_id, session=Depends(get_db))

    Change service calls:
      `await list_children()` → `await list_children(session)`
      `await get_child_by_id(child_id)` → `await get_child_by_id(child_id, session)`

    Note: child.py line 19 has a local import `from services.profiles import get_child_by_id` inside child_chat. Consolidate this to the top-level import block — remove the inline import, add `get_child_by_id` to the top-level `from services.profiles import` line.

    Edit api/parent.py:

    Add imports:
      from fastapi import Depends
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.session import get_db
      from services.sessions import get_session_history

    The existing code calls `get_all_sessions()` which no longer exists (removed when sessions.py was migrated — it was an in-memory dict dump). Replace with `get_session_history` pattern that fetches recent turns for all children, or simplify: for Phase 1, fetch children and pass an empty sessions dict to the template (session replay is Phase 4 scope).

    Simplest Phase 1 approach for api/parent.py:
      - Keep the children = await list_children(session) call
      - Replace `sessions = await get_all_sessions()` with `sessions = {}` and remove the get_all_sessions import
      - The dashboard.html template receives `sessions={}` — it must handle the empty dict without crashing. If the template iterates sessions, an empty dict is safe.

    Change route signature to include session:
      async def parent_dashboard(request: Request, session: AsyncSession = Depends(get_db)):

    Change service calls:
      `await list_children()` → `await list_children(session)`

    Edit api/dashboard.py:

    Add imports:
      from fastapi import Depends
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.session import get_db

    Same get_all_sessions problem: remove the import, replace with empty dict for Phase 1.

    Change route signature:
      async def dashboard(session: AsyncSession = Depends(get_db)):

    Change service calls:
      `await list_children()` → `await list_children(session)`

    Replace the serialisation. Current:
      return {
          "children": [vars(c) for c in children],
          "sessions": {k: [vars(t) for t in v] for k, v in sessions.items()},
      }

    Replace with:
      return {
          "children": [
              {
                  "id": c.id, "name": c.name, "age": c.age,
                  "device_id": c.device_id, "interests": c.interests,
                  "reading_level": c.reading_level, "current_topic": c.current_topic,
                  "current_books": c.current_books, "session_count": c.session_count,
                  "neurodivergence": c.neurodivergence,
              }
              for c in children
          ],
          "sessions": {},
      }

    After writing all three files: run `python -c "import api.child; import api.parent; import api.dashboard; print('OK')"`.
  </action>
  <verify>
    <automated>python -c "import api.child; import api.parent; import api.dashboard; print('OK')" &amp;&amp; python -m pytest tests/db/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import api.child; import api.parent; import api.dashboard; print('OK')"` prints "OK"
    - grep -c "Depends(get_db)" api/child.py returns 2 (one per route handler)
    - grep -c "Depends(get_db)" api/parent.py returns 1
    - grep -c "Depends(get_db)" api/dashboard.py returns 1
    - grep -c "vars(c)\|vars(t)" api/dashboard.py returns 0 (vars() removed)
    - grep -c "get_all_sessions" api/parent.py returns 0
    - grep -c "get_all_sessions" api/dashboard.py returns 0
    - api/parent.py and api/dashboard.py both use `sessions = {}` for Phase 1
    - pytest tests/db/ -x -q all pass
  </acceptance_criteria>
  <done>api/child.py, api/parent.py, api/dashboard.py all inject session; vars() removed; get_all_sessions removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → route handlers | child_id, device_id from URL path parameters — used as DB query params via ORM |
| ORM objects → JSON response | Explicit key enumeration prevents accidental leakage of internal SQLAlchemy state (_sa_instance_state) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-17 | Information Disclosure | dashboard.py serialises all child profiles in one response | accept | Phase 1 is dev-only; no auth on /v1/dashboard; Phase 4 adds parent auth gate |
| T-1-18 | Information Disclosure | api/sessions.py returns full question/answer history | accept | Scoped by child_id path param; no cross-child access; Phase 4 adds auth |
| T-1-19 | Tampering | api/sync.py device_id path param used for child lookup | mitigate | ORM parameterised query; returns empty payload for unknown device_id (not 404 — per existing code pattern) |
| T-1-20 | Information Disclosure | vars() removed — explicit key enumeration prevents _sa_instance_state leakage | mitigate | Explicit serialisation in dashboard.py and sessions.py ensures only declared fields are returned |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

  python -c "
  import api.sessions, api.sync, api.child, api.parent, api.dashboard
  print('All 5 route files import OK')
  "

  # Check no vars() remains in any api file
  grep -rn "vars(" api/ --include="*.py"
  # Expected: 0 matches

  # Check no get_all_sessions remains
  grep -rn "get_all_sessions" api/ --include="*.py"
  # Expected: 0 matches

  pytest tests/db/ -x -q
  # All green
</verification>

<success_criteria>
- All 5 route files import cleanly with no errors
- Every route handler that calls a service function now passes a session parameter via Depends(get_db)
- vars() usage replaced with explicit ORM serialisation in dashboard.py and sessions.py
- get_all_sessions removed from both api/parent.py and api/dashboard.py
- pytest tests/db/ -q: all green
- grep -rn "vars(" api/ returns 0 matches
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-07-SUMMARY.md` when done
</output>
