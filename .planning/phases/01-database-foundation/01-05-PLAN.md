---
phase: 01-database-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - services/profiles.py
  - services/sessions.py
  - api/stt.py
autonomous: true
requirements:
  - DB-02
  - DB-03
  - DB-04

must_haves:
  truths:
    - "services/profiles.py no longer has _profiles dict, _device_map dict, or ChildProfile dataclass"
    - "services/profiles.py no longer calls seed_dev_profiles() at module level — importing the module causes zero DB I/O"
    - "services/sessions.py no longer has _sessions dict or Turn dataclass"
    - "services/profiles.py functions delegate to db/crud.py and accept an explicit AsyncSession parameter"
    - "services/sessions.py log_turn and get_session_history accept an explicit AsyncSession parameter"
    - "api/stt.py does not import get_child_by_device_id (dead import removed)"
    - "Importing services.profiles causes no NameError, no DB I/O, and no side effects"
  artifacts:
    - path: "services/profiles.py"
      provides: "Thin wrapper over db/crud.py ChildProfile functions"
      contains: "from db.crud import"
    - path: "services/sessions.py"
      provides: "Thin wrapper over db/crud.py session/event functions"
      contains: "from db.crud import"
    - path: "api/stt.py"
      provides: "STT route with dead import removed"
  key_links:
    - from: "services/profiles.py:get_child_by_id"
      to: "db/crud.py:get_child_by_id"
      via: "thin delegation with same function name"
      pattern: "db_get_child_by_id"
    - from: "services/sessions.py:log_turn"
      to: "db/crud.py:log_turn"
      via: "thin delegation; session parameter required"
      pattern: "db_log_turn"
---

<objective>
Migrate services/profiles.py and services/sessions.py from in-memory dicts to thin wrappers over db/crud.py. Remove dead import from api/stt.py. After this plan, all service functions accept an explicit AsyncSession — the API route handlers (Plans 06-07) can then inject sessions via Depends(get_db).

Purpose: This is the integration boundary between DB layer and call chain. Services stay as a thin routing layer so existing callers (api/chat.py, api/child.py, etc.) only need to gain the session parameter — function names stay the same. Per D-01, D-03, D-10.

Output: Three modified files. All tests/db/ still pass. Importing services.profiles triggers no side effects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
@.planning/phases/01-database-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate services/profiles.py to thin wrapper</name>
  <files>services/profiles.py</files>
  <read_first>
    - services/profiles.py — read in full; this is the file being migrated
    - db/crud.py — read to confirm exact function signatures (create_child, get_child_by_id, get_child_by_device_id, list_children, update_interests) as written in Plans 02-03
    - services/tutor.py lines 200-230 — confirm all attribute accesses: child.age, child.name, child.interests, child.neurodivergence, child.current_topic, child.current_books, child.reading_level
    - .planning/phases/01-database-foundation/01-PATTERNS.md — section "services/profiles.py *(modify)*"
  </read_first>
  <behavior>
    - After migration, `import services.profiles` triggers no DB I/O and no side effects
    - `from services.profiles import get_child_by_id` is importable without error
    - get_child_by_id("child-kida", session) delegates to db/crud.get_child_by_id and returns a ChildProfileModel ORM instance
    - The ChildProfile dataclass is removed — callers that relied on duck typing still work because ORM model has same attribute names
  </behavior>
  <action>
    Replace services/profiles.py entirely. The new file:

    Imports:
      from typing import Optional
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.crud import (
          get_child_by_id as db_get_child_by_id,
          get_child_by_device_id as db_get_child_by_device_id,
          list_children as db_list_children,
          update_interests as db_update_interests,
      )

    DO NOT keep:
      - _profiles dict
      - _device_map dict
      - ChildProfile dataclass definition
      - seed_dev_profiles() function
      - The module-level `seed_dev_profiles()` call at line 71

    Keep the comment block describing neurodivergence flag values (lines 21-40 of current file) — move it to a module docstring or a comment near the top of the new file. This documentation is useful and should not be lost.

    Define these four functions as thin wrappers:

      async def get_child_by_device_id(device_id: str, session: AsyncSession):
          return await db_get_child_by_device_id(device_id, session)

      async def get_child_by_id(child_id: str, session: AsyncSession):
          return await db_get_child_by_id(child_id, session)

      async def list_children(session: AsyncSession):
          return await db_list_children(session)

      async def update_interests(child_id: str, new_interests: list[str], session: AsyncSession):
          return await db_update_interests(child_id, new_interests, session)

    Do NOT add any logic to these wrappers. They are pure delegation. If db/crud.py functions raise, let exceptions propagate — no try/except wrapping here.

    After writing: run `python -c "import services.profiles; print('OK')"` — must print OK with no errors and no DB I/O. If it raises ImportError, a module not found error, or creates a file in data/, stop and fix the import.
  </action>
  <verify>
    <automated>python -c "import services.profiles; print('OK')" &amp;&amp; python -m pytest tests/db/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import services.profiles; print('OK')"` prints "OK" (no side effects on import)
    - grep -c "_profiles" services/profiles.py returns 0 (in-memory dict removed)
    - grep -c "seed_dev_profiles" services/profiles.py returns 0 (seed function removed)
    - grep -c "class ChildProfile" services/profiles.py returns 0 (dataclass removed)
    - grep -c "from db.crud import" services/profiles.py returns 1 (imports from CRUD layer)
    - services/profiles.py contains get_child_by_id, get_child_by_device_id, list_children, update_interests — all four functions
    - pytest tests/db/ -x -q still all pass (migration of services/profiles.py does not break CRUD tests)
  </acceptance_criteria>
  <done>services/profiles.py is a thin wrapper with no in-memory state; importing it triggers no side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate services/sessions.py and remove stt.py dead import</name>
  <files>services/sessions.py, api/stt.py</files>
  <read_first>
    - services/sessions.py — read in full; this is the file being migrated
    - api/stt.py — read in full; confirms the dead import at line 6 (from services.profiles import get_child_by_device_id — unused in transcribe function body)
    - api/chat.py lines 58-60 — current log_turn call: `await log_turn(child_id, req.messages[-1].content, content)` — new signature must handle this call after Plan 06 adds session
    - db/crud.py — confirm log_turn and get_session_history signatures from Plan 03
    - .planning/phases/01-database-foundation/01-PATTERNS.md — section "services/sessions.py *(modify)*"
  </read_first>
  <behavior>
    - `import services.sessions` triggers no DB I/O and no side effects
    - log_turn(child_id, question, answer, session) delegates to db_log_turn and returns InteractionEventModel
    - get_session_history(child_id, session) returns list[InteractionEventModel]
    - api/stt.py transcribe function is unchanged except the dead import line is gone
  </behavior>
  <action>
    Replace services/sessions.py entirely. The new file:

    Imports:
      from typing import Optional
      from sqlalchemy.ext.asyncio import AsyncSession
      from db.crud import (
          log_turn as db_log_turn,
          get_session_history as db_get_session_history,
          create_session as db_create_session,
          get_session as db_get_session,
      )

    DO NOT keep:
      - _sessions dict
      - Turn dataclass definition

    Define these wrapper functions:

      async def log_turn(child_id: str, question: str, answer: str, session: AsyncSession, topic: str = None, session_id: str = None):
          return await db_log_turn(child_id, question, answer, session, topic=topic, session_id=session_id)

      async def get_session_history(child_id: str, session: AsyncSession, limit: int = 50):
          return await db_get_session_history(child_id, session, limit)

      async def create_session(child_id: str, session: AsyncSession):
          return await db_create_session(child_id, session)

      async def get_session(session_id: str, session: AsyncSession):
          return await db_get_session(session_id, session)

    Signature note: log_turn(child_id, question, answer, session, topic=None, session_id=None). The `session` argument is positional here. Plan 06 updates api/chat.py to pass it as the 4th positional arg. Keep topic and session_id as keyword-only to preserve backwards compatibility with existing callers who pass `topic` as a keyword.

    Edit api/stt.py: remove line 6 (`from services.profiles import get_child_by_device_id`). This is a dead import — the transcribe() function body does not call get_child_by_device_id. Removing it prevents the import from failing once services.profiles no longer exports ChildProfile-based functions without a session. Make no other changes to api/stt.py.

    After writing both files: run `python -c "import services.sessions; import api.stt; print('OK')"` — must print OK with no errors.
  </action>
  <verify>
    <automated>python -c "import services.sessions; import api.stt; print('OK')" &amp;&amp; python -m pytest tests/db/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import services.sessions; print('OK')"` prints "OK" (no side effects)
    - grep -c "_sessions" services/sessions.py returns 0 (in-memory dict removed)
    - grep -c "class Turn" services/sessions.py returns 0 (Turn dataclass removed)
    - grep -c "from db.crud import" services/sessions.py returns 1
    - grep -c "get_child_by_device_id" api/stt.py returns 0 (dead import removed)
    - api/stt.py still has the TranscriptionResponse class and transcribe function unchanged
    - pytest tests/db/ -x -q still all pass
  </acceptance_criteria>
  <done>services/sessions.py is a thin wrapper; Turn dataclass removed; api/stt.py dead import removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service function → crud function | Thin delegation; no additional processing; trust boundary is in crud layer (parameterised queries) |
| api/stt.py → services.profiles | Dead import removed; stt.py now has no profile service dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | Information Disclosure | Removing ChildProfile dataclass | accept | ORM model attribute names match exactly; tutor.py duck-typing still works; no type annotation change needed in tutor.py |
| T-1-12 | Tampering | services/sessions.py log_turn topic parameter | accept | topic is optional, internal call only; no user-facing validation needed at service layer in Phase 1 |
| T-1-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:

  python -c "import services.profiles; import services.sessions; import api.stt; print('All service imports OK')"

  grep -c "_profiles\|_sessions\|class ChildProfile\|class Turn\|seed_dev_profiles" services/profiles.py services/sessions.py
  # Must return 0 for all patterns

  grep -c "get_child_by_device_id" api/stt.py
  # Must return 0

  pytest tests/db/ -x -q
  # All tests still pass
</verification>

<success_criteria>
- services/profiles.py: no _profiles dict, no ChildProfile dataclass, no seed_dev_profiles, no module-level side effects
- services/sessions.py: no _sessions dict, no Turn dataclass
- api/stt.py: no import of get_child_by_device_id
- All functions in both service files accept AsyncSession as an explicit parameter
- pytest tests/db/ -q: all green (no regressions from service refactor)
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-05-SUMMARY.md` when done
</output>
