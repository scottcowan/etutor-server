---
phase: 01-database-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - alembic.ini
  - migrations/env.py
  - migrations/script.py.mako
  - migrations/versions/0001_initial_schema.py
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "alembic init has been run and migrations/ directory exists"
    - "migrations/env.py uses async engine (asyncio.run(run_async_migrations())) — NOT the synchronous default"
    - "alembic.ini sqlalchemy.url points to settings.database_url (sqlite+aiosqlite:///./data/etutor.db)"
    - "alembic revision --autogenerate creates a migration that includes all 4 tables: child_profiles, sessions, interaction_events, mastery_state"
    - "alembic upgrade head runs cleanly on a fresh SQLite file (Phase 1 scope — SQLite only)"
    - "PostgreSQL alembic upgrade head verification is deferred to Phase 6"
    - "alembic downgrade base runs cleanly (rollback verified)"
  artifacts:
    - path: "alembic.ini"
      provides: "Alembic configuration"
      contains: "sqlalchemy.url"
    - path: "migrations/env.py"
      provides: "Async Alembic env"
      contains: "run_async_migrations"
    - path: "migrations/versions/0001_initial_schema.py"
      provides: "Initial migration creating all 4 tables"
      contains: "child_profiles"
  key_links:
    - from: "migrations/env.py"
      to: "db/models.py Base.metadata"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata"
---

<objective>
Initialise Alembic migrations, replace the default synchronous env.py with the async pattern required by aiosqlite, and generate + verify the initial migration that creates all four tables.

Purpose: ROADMAP Phase 1 success criterion 2 requires `alembic upgrade head` to run cleanly on fresh SQLite. This plan satisfies the SQLite portion of that criterion. PostgreSQL verification is deferred to Phase 6 (production hardening) when a Postgres instance is available. It runs in parallel with Plan 02 (both depend only on Plan 01) because they touch disjoint files.

Output: migrations/ directory with working async env.py and an initial migration verified against a fresh SQLite file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
@.planning/phases/01-database-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: alembic init and async env.py replacement</name>
  <files>alembic.ini, migrations/env.py, migrations/script.py.mako</files>
  <read_first>
    - .planning/phases/01-database-foundation/01-RESEARCH.md — Pattern 7 (Alembic async env.py full code) and Pitfall 5 (synchronous default)
    - config/settings.py — to confirm settings.database_url default value for alembic.ini
    - db/models.py (just created in Plan 01) — confirm Base is importable as "from db.models import Base"
  </read_first>
  <action>
    Run `alembic init migrations` from the project root. This creates:
      - alembic.ini (project root)
      - migrations/env.py (default synchronous — MUST be replaced)
      - migrations/script.py.mako
      - migrations/versions/ (empty directory)

    After alembic init, edit alembic.ini: find the line `sqlalchemy.url = driver://user:pass@localhost/dbname` and replace it with:
      sqlalchemy.url = sqlite+aiosqlite:///./data/etutor.db

    This matches config/settings.py Settings.database_url default exactly. Do not hardcode a different path.

    Replace the entire body of migrations/env.py with the async pattern from RESEARCH.md Pattern 7. The complete replacement is:

      import asyncio
      from logging.config import fileConfig
      from sqlalchemy.ext.asyncio import async_engine_from_config
      from alembic import context

      config = context.config
      if config.config_file_name is not None:
          fileConfig(config.config_file_name)

      from db.models import Base
      target_metadata = Base.metadata

      def do_run_migrations(connection):
          context.configure(connection=connection, target_metadata=target_metadata)
          with context.begin_transaction():
              context.run_migrations()

      async def run_async_migrations():
          connectable = async_engine_from_config(
              config.get_section(config.config_ini_section),
              prefix="sqlalchemy.",
          )
          async with connectable.connect() as connection:
              await connection.run_sync(do_run_migrations)
          await connectable.dispose()

      def run_migrations_online():
          asyncio.run(run_async_migrations())

      if context.is_offline_mode():
          url = config.get_main_option("sqlalchemy.url")
          context.configure(url=url, target_metadata=target_metadata, literal_binds=True)
          with context.begin_transaction():
              context.run_migrations()
      else:
          run_migrations_online()

    CRITICAL: The default env.py generated by alembic init is synchronous and uses `engine_from_config` (not `async_engine_from_config`). Using it with sqlite+aiosqlite:// raises `TypeError: coroutine 'connect' was never awaited`. Replace the entire file; do not attempt to patch individual lines.

    Do NOT add "from db import Base" — import from db.models directly: "from db.models import Base". The db/__init__.py is currently empty (Plan 01 created it as a package marker only).

    Leave migrations/script.py.mako unchanged — it is a template for migration file generation and does not need modification for async.
  </action>
  <verify>
    <automated>python -c "from alembic import context; import migrations.env" 2>&amp;1 | grep -c "Error" | xargs -I{} test {} -eq 0 &amp;&amp; echo "OK" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - alembic.ini exists at project root
    - grep "sqlite+aiosqlite" alembic.ini returns a match
    - migrations/env.py contains "run_async_migrations" — verify with: grep -c "run_async_migrations" migrations/env.py
    - migrations/env.py contains "async_engine_from_config" — NOT "engine_from_config" (without async prefix)
    - migrations/env.py does NOT contain "engine_from_config" without "async_" prefix: grep -c "^from sqlalchemy import create_engine\|engine_from_config" migrations/env.py returns 0
    - `python -c "from db.models import Base; print(list(Base.metadata.tables.keys()))"` lists all 4 table names: child_profiles, sessions, interaction_events, mastery_state
  </acceptance_criteria>
  <done>alembic.ini has correct URL; migrations/env.py uses async pattern with run_async_migrations; env.py imports db.models.Base.</done>
</task>

<task type="auto">
  <name>Task 2: Generate initial migration and verify upgrade/downgrade</name>
  <files>migrations/versions/0001_initial_schema.py</files>
  <read_first>
    - alembic.ini (just written) — to confirm sqlalchemy.url before running commands
    - migrations/env.py (just written) — to verify async pattern is in place before autogenerate
    - db/models.py — all 4 models must be importable for autogenerate to detect them
  </read_first>
  <action>
    Ensure the data/ directory exists (it already does per project root check, and Plan 01 created data/.gitkeep). Run:

      alembic revision --autogenerate -m "initial schema"

    This creates migrations/versions/0001_initial_schema.py (or similar auto-named file). Inspect the generated file to confirm it contains upgrade() operations for all four tables: child_profiles, sessions, interaction_events, mastery_state.

    If any table is missing from the generated migration:
      - Confirm the model is defined in db/models.py (it should be from Plan 01)
      - Confirm migrations/env.py imports Base from db.models (check target_metadata = Base.metadata)
      - Re-run alembic revision --autogenerate after fixing the import

    Run the migration against a fresh test SQLite file to verify it executes cleanly:

      TMPDB=$(mktemp /tmp/etutor-test-XXXXXX.db)
      alembic -x test_db_url="sqlite+aiosqlite:///$TMPDB" upgrade head

    If alembic does not support -x for URL override at this level, instead:
      1. Temporarily set DATABASE_URL env var: `DATABASE_URL=sqlite+aiosqlite:///./data/etutor-test.db alembic upgrade head`
      2. Verify the file was created
      3. Run `alembic downgrade base` to verify rollback
      4. Delete the test db file

    Simplest approach: just run `alembic upgrade head` against the live data/etutor.db path (which matches alembic.ini). This creates data/etutor.db. Then run `alembic downgrade base`. Then `alembic upgrade head` again to leave the database in the upgraded state for development use.

    The data/etutor.db file is already in .gitignore (data/*.db) — no need to gitignore it separately.

    Confirm migration file is named with the revision ID prefix (e.g., `abc1234_initial_schema.py`). This is the file to commit. If alembic auto-named it differently, rename is not necessary — whatever autogenerate produces is fine.
  </action>
  <verify>
    <automated>alembic upgrade head 2>&amp;1 | tail -3 &amp;&amp; alembic current 2>&amp;1 | grep -c "head"</automated>
  </verify>
  <acceptance_criteria>
    - migrations/versions/ contains exactly one .py file after autogenerate
    - The migration file contains `op.create_table('child_profiles'` — verify with: grep -c "child_profiles" migrations/versions/*.py
    - The migration file contains `op.create_table('sessions'`, `op.create_table('interaction_events'`, `op.create_table('mastery_state'` — verify by grepping the generated file
    - `alembic upgrade head` exits 0 (SQLite only; PostgreSQL verification deferred to Phase 6)
    - `alembic current` output contains "(head)" after upgrade
    - `alembic downgrade base` exits 0 (rollback succeeds)
    - `alembic upgrade head` runs again without error after downgrade (idempotent migration path)
  </acceptance_criteria>
  <done>Initial migration generated with all 4 tables; alembic upgrade head and downgrade base both succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic.ini → data/etutor.db | Migration commands read URL from alembic.ini; file path is local to server; no network involved |
| env.py → db.models.Base | env.py imports Base at module load; Base.metadata defines the schema authoritative source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Information Disclosure | alembic.ini sqlalchemy.url contains database path | accept | URL is a local file path, no credentials; alembic.ini should not be committed with production credentials — add `config/.env` override note if PostgreSQL URL needed in prod |
| T-1-10 | Tampering | alembic downgrade base deletes all data | accept | downgrade base is a developer-only command; never run in production; not wired to any API endpoint |
| T-1-SC | Tampering | alembic autogenerate writes a new .py file | mitigate | Generated migration file must be reviewed before committing — executor reviews it for all 4 tables before proceeding to upgrade |
</threat_model>

<verification>
After both tasks complete:

  alembic current
  # Must print: <rev_hash> (head)

  python -c "
  from db.models import Base
  tables = list(Base.metadata.tables.keys())
  expected = {'child_profiles', 'sessions', 'interaction_events', 'mastery_state'}
  assert set(tables) == expected, f'Missing: {expected - set(tables)}'
  print('All 4 tables in metadata:', tables)
  "

  grep -c "create_table" migrations/versions/*.py
  # Must be >= 4 (one create_table per model)
</verification>

<success_criteria>
- alembic.ini sqlalchemy.url = sqlite+aiosqlite:///./data/etutor.db
- migrations/env.py uses async pattern (run_async_migrations)
- alembic upgrade head creates all 4 tables on SQLite: child_profiles, sessions, interaction_events, mastery_state
- alembic downgrade base succeeds on SQLite (rollback verified)
- alembic current shows "(head)" after upgrade on SQLite
- NOTE: PostgreSQL upgrade head verification is out of scope for Phase 1; deferred to Phase 6 (production hardening)
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-04-SUMMARY.md` when done
</output>
