---
phase: 01-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - data/.gitkeep
  - db/__init__.py
  - db/models.py
  - db/session.py
  - db/seeds.py
  - tests/conftest.py
  - tests/db/__init__.py
autonomous: true
requirements:
  - DB-01

must_haves:
  truths:
    - "pytest.ini exists with asyncio_mode = auto"
    - "db/models.py defines Base, ChildProfileModel, SessionModel, InteractionEventModel, MasteryStateModel"
    - "db/session.py exports AsyncEngine, AsyncSessionLocal, get_db as a FastAPI dependency generator"
    - "db/seeds.py defines async seed_dev_data(session) that upserts KidA and KidB profiles"
    - "tests/conftest.py defines a db_session fixture backed by in-memory SQLite with all tables created via create_all"
    - "Importing db.models does not trigger any DB I/O"
    - "Importing services.profiles does not trigger any DB I/O (module-level seed call still present — that is removed in Plan 05)"
  artifacts:
    - path: "pytest.ini"
      provides: "asyncio_mode = auto config for pytest-asyncio"
      contains: "asyncio_mode"
    - path: "db/models.py"
      provides: "SQLAlchemy ORM models"
      contains: "class ChildProfileModel"
    - path: "db/session.py"
      provides: "AsyncEngine + get_db dependency"
      contains: "get_db"
    - path: "db/seeds.py"
      provides: "Idempotent KidA/KidB seed function"
      contains: "seed_dev_data"
    - path: "tests/conftest.py"
      provides: "db_session fixture"
      contains: "db_session"
  key_links:
    - from: "db/session.py"
      to: "config/settings.py"
      via: "get_settings().database_url"
      pattern: "get_settings"
    - from: "db/models.py"
      to: "services/tutor.py attribute access"
      via: "column names must match ChildProfile dataclass field names"
      pattern: "child\\.interests|child\\.neurodivergence|child\\.current_topic|child\\.current_books"
---

<objective>
Bootstrap the entire database layer: install dependencies, create the db/ package with ORM models, async session factory, and dev seed function, and stand up the pytest-asyncio test infrastructure.

Purpose: Everything else in Phase 1 depends on these files existing. Plan 02 writes tests against db/crud.py; those tests need the db_session fixture from this plan. Plan 03 extends crud.py; it also needs the fixture. Plans 05-07 need models and session factory in place before they can thread sessions through the call chain.

Output: db/ package, tests/conftest.py, pytest.ini, data/.gitkeep — no CRUD functions yet (those land in Plans 02-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create pytest.ini + data/.gitkeep</name>
  <files>pytest.ini, data/.gitkeep</files>
  <read_first>
    - requirements.txt — confirm sqlalchemy, alembic, aiosqlite are listed (they are; verify exact specifiers)
    - .gitignore — confirm data/*.db is ignored but data/ directory itself is not
  </read_first>
  <action>
    Install the four packages that are in requirements.txt but not in the venv:

      pip install "sqlalchemy>=2.0.0" "alembic>=1.13.0" "aiosqlite>=0.20.0" pytest-asyncio

    Verify each installs without conflict (pip will print a success line). If pip reports a conflict, record the conflict and abort — do not proceed with conflicting packages.

    Create pytest.ini at project root with exactly this content (no other keys — adding testpaths or other config risks breaking the existing evals):

      [pytest]
      asyncio_mode = auto

    Create data/.gitkeep as an empty file. The data/ directory already exists; this file ensures git tracks the directory so SQLite has a home on a fresh clone. Do NOT add data/.gitkeep to .gitignore.

    Verify: python -c "import sqlalchemy; import alembic; import aiosqlite; import pytest_asyncio; print('OK')" must print "OK".
  </action>
  <verify>
    <automated>python -c "import sqlalchemy, alembic, aiosqlite, pytest_asyncio; print('OK')" 2>&amp;1 | grep -c OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import sqlalchemy, alembic, aiosqlite, pytest_asyncio; print('OK')"` prints "OK" with exit code 0
    - pytest.ini exists at project root and contains the line `asyncio_mode = auto`
    - data/.gitkeep exists as an empty file
    - No other files in the project are modified by this task
  </acceptance_criteria>
  <done>All four packages importable; pytest.ini contains asyncio_mode = auto; data/.gitkeep exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create db/ package — models and session factory</name>
  <files>db/__init__.py, db/models.py, db/session.py</files>
  <read_first>
    - services/profiles.py — read lines 1-40 for the ChildProfile dataclass field names; ORM column names MUST match exactly
    - services/sessions.py — read lines 1-16 for the Turn dataclass field names
    - config/settings.py — read in full; db/session.py calls get_settings() for database_url
    - .planning/phases/01-database-foundation/01-PATTERNS.md — sections "db/models.py" and "db/session.py" for authoritative patterns
    - .planning/phases/01-database-foundation/01-RESEARCH.md — Patterns 1, 2, 9 for ORM model and session factory
  </read_first>
  <action>
    Create db/__init__.py as an empty file (package marker only — no re-exports yet; callers import from db.models, db.session directly).

    Create db/models.py with these four ORM models. Use SQLAlchemy 2.0 DeclarativeBase style with Mapped[] typed annotations. Import: String, Integer, Boolean, JSON, DateTime, Float, ForeignKey from sqlalchemy; DeclarativeBase, mapped_column, Mapped from sqlalchemy.orm; datetime from datetime; Optional from typing.

    Model 1 — ChildProfileModel (__tablename__ = "child_profiles"):
      id: Mapped[str] = mapped_column(String, primary_key=True)
      name: Mapped[str] = mapped_column(String, nullable=False)
      age: Mapped[int] = mapped_column(Integer, nullable=False)
      device_id: Mapped[Optional[str]] = mapped_column(String, nullable=True, index=True)
      reading_level: Mapped[str] = mapped_column(String, default="age-appropriate")
      interests: Mapped[list] = mapped_column(JSON, default=list)
      neurodivergence: Mapped[list] = mapped_column(JSON, default=list)
      current_topic: Mapped[Optional[str]] = mapped_column(String, nullable=True)
      current_books: Mapped[list] = mapped_column(JSON, default=list)
      session_count: Mapped[int] = mapped_column(Integer, default=0)

    CRITICAL: column names above must exactly match services/profiles.py ChildProfile dataclass field names. services/tutor.py accesses child.interests, child.neurodivergence, child.current_topic, child.current_books, child.name, child.age, child.reading_level via attribute access (lines 208-228). A name mismatch causes AttributeError at runtime.

    Model 2 — SessionModel (__tablename__ = "sessions"):
      id: Mapped[str] = mapped_column(String, primary_key=True)  # UUID4
      child_id: Mapped[str] = mapped_column(String, ForeignKey("child_profiles.id"), nullable=False, index=True)
      started_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
      ended_at: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
      # turn_count is derived — do not store as a column; compute from interaction_events

    Model 3 — InteractionEventModel (__tablename__ = "interaction_events"):
      id: Mapped[str] = mapped_column(String, primary_key=True)  # UUID4
      child_id: Mapped[str] = mapped_column(String, ForeignKey("child_profiles.id"), nullable=False, index=True)
      session_id: Mapped[Optional[str]] = mapped_column(String, ForeignKey("sessions.id"), nullable=True)
      question: Mapped[str] = mapped_column(String, nullable=False)
      answer: Mapped[str] = mapped_column(String, nullable=False)
      topic: Mapped[Optional[str]] = mapped_column(String, nullable=True)
      timestamp: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)
      # DB-04 scaffold columns — nullable in Phase 1; populated in Phase 2 (BKT) and Phase 1 chat timing
      kc_id: Mapped[Optional[str]] = mapped_column(String, nullable=True)   # set by Phase 2 BKT
      correct: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)  # set by Phase 2 BKT
      response_ms: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)  # chat turn latency
      hint_used: Mapped[Optional[bool]] = mapped_column(Boolean, nullable=True)   # set by Phase 2

    Model 4 — MasteryStateModel (__tablename__ = "mastery_state", composite PK):
      child_id: Mapped[str] = mapped_column(String, ForeignKey("child_profiles.id"), primary_key=True)
      kc_id: Mapped[str] = mapped_column(String, primary_key=True)
      p_mastery: Mapped[float] = mapped_column(Float, default=0.1)
      p_learn: Mapped[float] = mapped_column(Float, default=0.2)
      p_slip: Mapped[float] = mapped_column(Float, default=0.1)
      p_guess: Mapped[float] = mapped_column(Float, default=0.2)
      stability: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      difficulty_d: Mapped[Optional[float]] = mapped_column(Float, nullable=True)
      card_state: Mapped[Optional[str]] = mapped_column(String, nullable=True)
      next_review: Mapped[Optional[datetime]] = mapped_column(DateTime, nullable=True)
      updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.utcnow)

    Create db/session.py. Imports: create_async_engine, AsyncSession, async_sessionmaker from sqlalchemy.ext.asyncio; get_settings from config.settings.

    Module body:
      settings = get_settings()
      engine = create_async_engine(settings.database_url, echo=False)
      AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

      async def get_db():
          async with AsyncSessionLocal() as session:
              yield session

    Do NOT use lru_cache on get_db — it is a generator and caching it would reuse exhausted generators. get_settings() is already lru_cache-wrapped; calling it at module level here is safe.
  </action>
  <verify>
    <automated>python -c "from db.models import Base, ChildProfileModel, SessionModel, InteractionEventModel, MasteryStateModel; from db.session import engine, AsyncSessionLocal, get_db; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from db.models import Base, ChildProfileModel, SessionModel, InteractionEventModel, MasteryStateModel` succeeds with no error
    - `from db.session import engine, AsyncSessionLocal, get_db` succeeds with no error
    - ChildProfileModel has columns: id, name, age, device_id, reading_level, interests, neurodivergence, current_topic, current_books, session_count — verify with `python -c "from db.models import ChildProfileModel; print([c.key for c in ChildProfileModel.__table__.columns])"`
    - MasteryStateModel.__table__.primary_key has two columns: child_id and kc_id
    - InteractionEventModel has nullable columns kc_id, correct, response_ms, hint_used — verify with:  
    - Importing db.models does not connect to the database (no OperationalError, no file created in data/ just from import)
  </acceptance_criteria>
  <done>db/models.py and db/session.py importable; all 4 ORM models defined with correct column names; get_db is a generator function.</done>
</task>

<task type="auto">
  <name>Task 3: Create db/seeds.py and tests/conftest.py + tests/db/__init__.py</name>
  <files>db/seeds.py, tests/conftest.py, tests/db/__init__.py</files>
  <read_first>
    - services/profiles.py lines 42-71 — the existing seed_dev_profiles() structure to mirror (using KidA/KidB per D-08)
    - .planning/phases/01-database-foundation/01-RESEARCH.md — Pattern 4 (idempotent upsert), Pattern 5 (pytest fixture)
    - .planning/phases/01-database-foundation/01-PATTERNS.md — sections "db/seeds.py" and "tests/conftest.py"
    - db/models.py (just written) — to confirm ChildProfileModel column names for the seed insert statement
  </read_first>
  <action>
    Create db/seeds.py. Imports: AsyncSession from sqlalchemy.ext.asyncio; insert as sqlite_insert from sqlalchemy.dialects.sqlite; ChildProfileModel from db.models.

    Define `async def seed_dev_data(session: AsyncSession) -> None`. Insert two profiles using INSERT OR IGNORE (idempotent — runs on every startup without error):

    Profile 1 (KidA — per D-08, no real names):
      id="child-kida", name="KidA", age=7, device_id="device-001",
      interests=["dinosaurs", "space", "Minecraft"], reading_level="grade 2",
      current_topic="prehistoric life", current_books=[], session_count=0, neurodivergence=[]

    Profile 2 (KidB — per D-08):
      id="child-kidb", name="KidB", age=10, device_id="device-002",
      interests=["volcanoes", "ocean animals", "coding"], reading_level="grade 5",
      current_topic="earth science", current_books=[], session_count=0, neurodivergence=[]

    For each, use: `stmt = sqlite_insert(ChildProfileModel).values(...).prefix_with("OR IGNORE")` then `await session.execute(stmt)`. Call `await session.commit()` once after both inserts.

    CRITICAL: Use generic identifiers only. Do not use "Alex", "Sam", or any real name. This file will be read by anyone who clones the repo.

    Create tests/db/__init__.py as an empty file.

    Create tests/conftest.py. Imports: pytest_asyncio; create_async_engine, AsyncSession, async_sessionmaker from sqlalchemy.ext.asyncio; Base from db.models.

    Define `@pytest_asyncio.fixture async def db_session()`:
      1. Create in-memory engine: `create_async_engine("sqlite+aiosqlite:///:memory:", echo=False)`
      2. Run create_all: `async with engine.begin() as conn: await conn.run_sync(Base.metadata.create_all)`
      3. Create session factory: `async_sessionmaker(engine, expire_on_commit=False)`
      4. Yield a session: `async with factory() as session: yield session`
      5. Dispose after yield: `await engine.dispose()`

    Use Base.metadata.create_all (not Alembic programmatic invocation) for test fixture simplicity. This is correct for Phase 1 — the schema comes from the same models that Alembic will autogenerate from, so create_all and the migration produce identical schemas.

    Deviation note (D-09): D-09 specifies that the eval fixture "applies Alembic migrations". create_all is used here instead because it produces an identical schema for Phase 1 (single initial migration) while being simpler and faster for the test fixture lifecycle. Programmatic Alembic invocation adds complexity with no schema correctness benefit in Phase 1. If D-09 must be honoured literally in future phases, update CONTEXT.md to allow create_all explicitly, or add a separate test fixture variant that runs alembic upgrade head programmatically.

    Do NOT add an `asyncio_mode` key to conftest.py — that belongs only in pytest.ini (already created in Task 1).
  </action>
  <verify>
    <automated>python -c "from db.seeds import seed_dev_data; from tests.conftest import db_session; print('OK')" 2>&amp;1 | grep -c OK</automated>
  </verify>
  <acceptance_criteria>
    - `from db.seeds import seed_dev_data` succeeds; seed_dev_data is an async function (inspect.iscoroutinefunction returns True)
    - db/seeds.py contains "KidA" and "KidB" — verify with grep
    - db/seeds.py does NOT contain "Alex" or "Sam" — grep for those strings must return empty
    - tests/conftest.py defines db_session as a pytest_asyncio fixture using in-memory SQLite
    - tests/db/__init__.py exists (may be empty)
    - `python -m pytest tests/db/ --collect-only -q` reports 0 tests collected and 0 errors (no syntax errors in conftest)
  </acceptance_criteria>
  <done>db/seeds.py has idempotent KidA+KidB upsert; tests/conftest.py has working db_session fixture; tests/db package exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import-time → runtime | db/session.py creates engine at import time using settings.database_url — malformed URL causes OperationalError at startup, not import |
| test fixture → in-memory SQLite | db_session fixture uses "sqlite+aiosqlite:///:memory:" — completely isolated, no filesystem access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | db/session.py engine creation | mitigate | database_url comes only from config/.env or environment variable via pydantic-settings; never from user input |
| T-1-02 | Information Disclosure | db/seeds.py seed data | mitigate | D-08: use generic KidA/KidB identifiers; no real child names in code per CONTEXT.md decision |
| T-1-03 | Information Disclosure | data/etutor.db file permissions | accept | Dev environment; .gitignore already excludes data/*.db; filesystem permissions managed by OS; production hardening is Phase 6 scope |
| T-1-SC | Tampering | pip install sqlalchemy alembic aiosqlite pytest-asyncio | mitigate | Package Legitimacy Audit in RESEARCH.md confirms all four packages approved; all are in requirements.txt (pre-existing project dependency declaration) |
</threat_model>

<verification>
After all three tasks complete:

  python -c "from db.models import Base, ChildProfileModel, SessionModel, InteractionEventModel, MasteryStateModel; from db.session import engine, AsyncSessionLocal, get_db; from db.seeds import seed_dev_data; print('All DB imports OK')"

  python -m pytest tests/db/ --collect-only -q

  grep -c "asyncio_mode" pytest.ini

  grep -v "Alex\|Sam" db/seeds.py | grep -c "KidA\|KidB"
</verification>

<success_criteria>
- All four packages importable: sqlalchemy, alembic, aiosqlite, pytest_asyncio
- db/ package provides Base, all 4 ORM models, engine, AsyncSessionLocal, get_db, seed_dev_data
- ChildProfileModel column names exactly match ChildProfile dataclass fields (id, name, age, device_id, interests, reading_level, current_topic, current_books, session_count, neurodivergence)
- pytest.ini has asyncio_mode = auto
- tests/conftest.py db_session fixture creates in-memory SQLite with all tables
- db/seeds.py uses KidA/KidB only — zero occurrences of "Alex" or "Sam"
</success_criteria>

<output>
Create `.planning/phases/01-database-foundation/01-01-SUMMARY.md` when done
</output>
