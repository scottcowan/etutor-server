---
phase: 03-session-intelligence
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - services/tutor.py
  - services/knowledge_tracing.py
  - api/chat.py
  - api/sessions.py
  - tests/services/test_tutor.py
  - tests/api/test_session_turns.py
  - tests/api/test_session_end.py
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - CURR-01
  - CURR-02
  - CURR-03

must_haves:
  truths:
    - "GET /sessions/{session_id}/turns returns 200 with a turns list scoped to that session_id (HIST-03)"
    - "The system prompt for a child with 24h interaction history includes 'Recent topics:' block (HIST-01)"
    - "The system prompt for a child with fragile/not_started prereqs includes a prereq tree block with escalation signal (HIST-02, D-07)"
    - "POST /sessions/{id}/end triggers extract_and_update_interests() for the session (CURR-03 fast path)"
    - "POST /chat/completions triggers extract_and_update_interests() for the previous ended session (CURR-03 catch-up path, D-08)"
    - "build_system_prompt() existing callers are unaffected — new params default to None"
    - "system prompt for chat() includes a focus_topics line derived from next_topics(child_id) filtered/ordered by child.interests (CURR-01)"
    - "system prompt includes a prerequisite guidance block (prereq_tree + turn-calibrated escalation signal) for at least one fragile/not_started prerequisite when one exists (CURR-02, D-07)"
    - "increment_prereq_turn() is called once per chat turn for each entry in prereq_tree (D-07 escalation counter advances)"
  artifacts:
    - path: "services/tutor.py"
      provides: "build_system_prompt extended with history_context, prereq_tree, session_prereq_state params"
      contains: "history_context: Optional"
    - path: "api/sessions.py"
      provides: "GET /sessions/{session_id}/turns endpoint + interest extraction in end_session()"
      contains: "get_session_turns"
    - path: "api/chat.py"
      provides: "Wired intelligence calls before build_system_prompt()"
      contains: "build_24hr_history_context"
  key_links:
    - from: "api/chat.py"
      to: "services/session_intelligence.py"
      via: "build_24hr_history_context, build_prereq_tree_context, get_session_prereq_state, increment_prereq_turn"
      pattern: "build_24hr_history_context"
    - from: "api/chat.py"
      to: "services/tutor.py"
      via: "build_system_prompt with new params"
      pattern: "history_context=history_ctx"
    - from: "api/sessions.py"
      to: "services/session_intelligence.py"
      via: "extract_and_update_interests"
      pattern: "extract_and_update_interests"
    - from: "services/tutor.py"
      to: "services/session_intelligence.py"
      via: "_format_escalation_signal reads turn count from session_prereq_state dict keyed by prereq_kc_id"
      pattern: "_format_escalation_signal"
    - from: "api/chat.py"
      to: "services/knowledge_tracing.next_topics"
      via: "next_topics result filtered by child.interests drives focus_topics in system prompt (CURR-01)"
      pattern: "next_topics"
---

<objective>
Wire all Phase 3 session intelligence into the API layer. This plan extends three existing files (services/tutor.py, api/chat.py, api/sessions.py) and adds the HIST-03 endpoint. All service functions were built in Plans 01-03; this plan connects them.

Purpose: Make the Phase 3 capabilities observable — they were implemented in the service layer but have no effect until wired into the request/response path.
Output: Extended tutor.py, extended chat.py, extended sessions.py (interest extraction + HIST-03 endpoint), and integration tests.
</objective>

<execution_context>
@/Users/scowan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scowan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-intelligence/03-CONTEXT.md
@.planning/phases/03-session-intelligence/03-RESEARCH.md
@.planning/phases/03-session-intelligence/03-PATTERNS.md
@.planning/phases/03-session-intelligence/03-01-SUMMARY.md
@.planning/phases/03-session-intelligence/03-02-SUMMARY.md
@.planning/phases/03-session-intelligence/03-03-SUMMARY.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend build_system_prompt() with history_context, prereq_tree, session_prereq_state params</name>
  <files>services/tutor.py, tests/services/test_tutor.py</files>

  <read_first>
    - services/tutor.py — read the FULL file before editing; specifically: (a) current build_system_prompt() signature at line ~242; (b) the MASTERY_CONTEXT_TEMPLATE and _format_mastery_context() pattern (lines ~190–218) as the exact template to follow for two new formatter functions; (c) the return statement at the end of build_system_prompt() (currently `return base_prompt + mastery_section`) that must be extended
    - tests/services/test_tutor.py — read the full file; find _make_child() helper (lines 21–33) and existing backward-compat tests for build_system_prompt to ensure the new params don't break them
  </read_first>

  <behavior>
    RED tests to add in tests/services/test_tutor.py:

    Test: test_system_prompt_with_history_context
    - child = _make_child()
    - Call `await build_system_prompt(child, history_context=["volcanoes", "plate tectonics"])`
    - Assert: "Recent topics: volcanoes, plate tectonics" in result (the exact format from D-01)

    Test: test_system_prompt_with_prereq_tree
    - child = _make_child()
    - Call `await build_system_prompt(child, prereq_tree=[{"prereq_name": "Rock Types", "prereq_kc_id": "rocks_soils", "unlocks": ["Volcanoes", "Plate Tectonics"]}])`
    - Assert: "Rock Types" in result
    - Assert: "Volcanoes" in result

    Test: test_system_prompt_prereq_escalation_turn1 (D-07)
    - child = _make_child()
    - prereq_entry = {"prereq_name": "Rock Types", "prereq_kc_id": "rocks_soils", "unlocks": ["Volcanoes"]}
    - state_turn1 = {"rocks_soils": 1}
    - result = await build_system_prompt(child, prereq_tree=[prereq_entry], session_prereq_state=state_turn1)
    - Assert: "Turn 1" in result or "engage" in result (Turn 1 instruction present)

    Test: test_system_prompt_prereq_escalation_turn3 (D-07)
    - child = _make_child()
    - prereq_entry = {"prereq_name": "Rock Types", "prereq_kc_id": "rocks_soils", "unlocks": ["Volcanoes"]}
    - state_turn3 = {"rocks_soils": 3}
    - result = await build_system_prompt(child, prereq_tree=[prereq_entry], session_prereq_state=state_turn3)
    - Assert: "Turn 3" in result or "steer" in result (active steer signal at turn 3+)

    Test: test_system_prompt_backward_compat_no_new_params
    - child = _make_child()
    - Call `await build_system_prompt(child)` (no new params)
    - Assert: result is a non-empty string (does not crash)
    - Assert: "Recent topics" not in result (history section absent when no history_context)

    GREEN implementation in services/tutor.py:

    1. Extend build_system_prompt() signature (after mastery_context param, all with None defaults):
           async def build_system_prompt(
               child,
               mastery_context: Optional[list] = None,
               history_context: Optional[list] = None,
               prereq_tree: Optional[list] = None,
               session_prereq_state: Optional[dict] = None,
           ) -> str:
       Use `Optional[list]` / `Optional[dict]` — not `list | None` (Python 3.9 project constraint).

    2. Add template constant after MASTERY_CONTEXT_TEMPLATE:
           HISTORY_CONTEXT_TEMPLATE = "\nRecent topics: {topic_list}\n"

    3. Add private formatter after _format_mastery_context():
           def _format_history_context(history_context: list) -> str:
               if not history_context:
                   return ""
               return HISTORY_CONTEXT_TEMPLATE.format(topic_list=", ".join(history_context))

    4. Add prereq tree template, formatter, and escalation signal formatter (D-07):
           PREREQ_TREE_TEMPLATE = "\nPrerequisite gaps:\n{prereq_lines}\n"

           # Maps turn count (1-indexed) to the instruction text appended per prereq line.
           _ESCALATION_INSTRUCTIONS = {
               1: "(Turn 1 — engage and hint: gently acknowledge the gap, offer a clue)",
               2: "(Turn 2 — gentle probe: ask a question to check what the child knows)",
           }
           _ESCALATION_STEER = "(Turn 3+ — active steer: redirect the child to address this prerequisite first)"

           def _format_escalation_signal(prereq_tree: list, session_prereq_state: Optional[dict]) -> str:
               """
               D-07: For each prereq entry, look up the turn count from session_prereq_state
               using entry["prereq_kc_id"] as the counter key (set by Plan 03
               build_prereq_tree_context). Falls back to Turn 1 if state is absent.
               session_prereq_state is keyed by kc_id → turn_count (int).
               """
               if not prereq_tree:
                   return ""
               lines = []
               state = session_prereq_state or {}
               for entry in prereq_tree:
                   unlocks_str = ", ".join(entry.get("unlocks", []))
                   kc_id = entry.get("prereq_kc_id", "")
                   turn_count = state.get(kc_id, 1)
                   if turn_count >= 3:
                       escalation = _ESCALATION_STEER
                   else:
                       escalation = _ESCALATION_INSTRUCTIONS.get(turn_count, _ESCALATION_INSTRUCTIONS[1])
                   lines.append(f"- {entry['prereq_name']} → unlocks: {unlocks_str}  {escalation}")
               if not lines:
                   return ""
               return PREREQ_TREE_TEMPLATE.format(prereq_lines="\n".join(lines))

           def _format_prereq_tree(prereq_tree: list, session_prereq_state: Optional[dict] = None) -> str:
               """Renders prereq tree with D-07 escalation signals embedded per line."""
               return _format_escalation_signal(prereq_tree, session_prereq_state)

    5. Extend the return statement in build_system_prompt():
           mastery_section = _format_mastery_context(mastery_context) if mastery_context else ""
           history_section = _format_history_context(history_context) if history_context else ""
           prereq_section = _format_prereq_tree(prereq_tree, session_prereq_state) if prereq_tree else ""
           return base_prompt + mastery_section + history_section + prereq_section

    Existing callers passing `build_system_prompt(child)` or `build_system_prompt(child, mastery_context=[...])` are unaffected by the None defaults. The `session_prereq_state` param is now actively used by `_format_prereq_tree` to embed the D-07 escalation signal — this is NOT deferred to a future phase.

    D-06 refresher wiring decision: D-06 requires the tutor to give a brief refresher when a correct probe answer is received. The refresher signal is handled by the Turn 3+ active-steer escalation language in `_ESCALATION_STEER` and the Turn 2 gentle-probe text — these already imply the child should revisit the prerequisite before proceeding. A dedicated `refresher_signal` param is NOT added here because: (a) reset_prereq_turn() is called in Task 3 (api/chat.py) when correct=True for a prereq KC, which clears the counter so the next turn starts fresh at Turn 1; (b) the Turn 1 "engage and hint" language naturally drives the refresher on re-engagement; (c) adding an out-of-band refresher_signal would require api/chat.py to pass correct-answer context into build_system_prompt, coupling the prompt builder to BKT state it should not own. This choice is acceptable per D-06: the escalation language ("Let's build up to this") covers the refresher intent. [informational — D-06b]
  </behavior>

  <verify>
    <automated>pytest tests/services/test_tutor.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/services/test_tutor.py -x -q` exits 0 (all existing + new tests pass)
    - `grep -c "history_context: Optional" services/tutor.py` returns 1
    - `grep -c "prereq_tree: Optional" services/tutor.py` returns 1
    - `grep -c "HISTORY_CONTEXT_TEMPLATE" services/tutor.py` returns at least 2 (definition + usage)
    - `pytest tests/evals/ -x -q` exits 0 (existing evals unaffected)
  </acceptance_criteria>

  <done>build_system_prompt() accepts three new Optional params with None defaults. History context renders as "Recent topics: X, Y". Prereq tree renders per-line D-07 escalation signals: Turn 1 = engage+hint, Turn 2 = gentle probe, Turn 3+ = active steer. Lookup uses entry["prereq_kc_id"] — matches the counter key set by increment_prereq_turn(). All existing callers unaffected.</done>
</task>

<task type="execute">
  <name>Task 2: Add GET /sessions/{session_id}/turns endpoint (HIST-03) + wire interest extraction in end_session()</name>
  <files>api/sessions.py, tests/api/test_session_turns.py, tests/api/test_session_end.py</files>

  <read_first>
    - api/sessions.py — read the FULL file; specifically: (a) existing `get_sessions()` endpoint (lines 14–35) as the exact structural analog for the new HIST-03 endpoint; (b) `end_session()` function body to find the exact insertion point (after `fit_fsrs_params()` call, before `return`); (c) existing import block to plan additions
    - tests/api/test_session_end.py — read to understand the full integration test fixture stack (`mem_engine`, `mem_session`, `test_client`, `test_data` fixtures); the new test files replicate this stack exactly
    - db/crud.py — confirm `get_turns_by_session_id` signature (from Plan 01): `async def get_turns_by_session_id(session_id: str, db: AsyncSession) -> list[InteractionEventModel]`
  </read_first>

  <action>
    PART A — HIST-03 endpoint in api/sessions.py:

    Add import at the top of api/sessions.py:
        from db.crud import get_turns_by_session_id
        from services.session_intelligence import extract_and_update_interests

    Add the new GET endpoint after the existing get_sessions() endpoint (route path `/sessions/{session_id}/turns` — the `/turns` suffix prevents any path conflict with `/sessions/{child_id}`):

        @router.get("/sessions/{session_id}/turns")
        async def get_session_turns(
            session_id: str,
            db: AsyncSession = Depends(get_db),
        ):
            # HIST-03 (CR-02 note): same IDOR exposure as GET /sessions/{child_id} — fix both together in auth phase
            turns = await get_turns_by_session_id(session_id, db)
            return {
                "session_id": session_id,
                "turns": [
                    {
                        "id": t.id,
                        "child_id": t.child_id,
                        "question": t.question,
                        "answer": t.answer,
                        "topic": t.topic,
                        "timestamp": t.timestamp.isoformat() if t.timestamp else None,
                    }
                    for t in turns
                ],
            }

    PART B — Interest extraction in end_session():

    In end_session(), after the `await fit_fsrs_params(session_row.child_id, db)` line and before the `return` statement, insert:
        await extract_and_update_interests(session_id, session_row.child_id, db)

    This uses the `db: AsyncSession` already in scope (from `Depends(get_db)`) — no new dependency injection needed. This is NOT the streaming generator path (CR-01 non-regression: extract_and_update_interests only runs after the HTTP response lifecycle for end_session, not inside generate()).

    PART C — Integration tests in tests/api/test_session_turns.py:

    Create tests/api/test_session_turns.py. Copy the full fixture stack from tests/api/test_session_end.py (mem_engine, mem_session, test_client, test_data). The test_data fixture for this file should: create a child, create a session, log 2 turns under that session_id using log_turn().

    Add test:
        async def test_get_session_turns_200(test_client, test_data):
            child_id, session_id = test_data
            response = await test_client.get(f"/v1/sessions/{session_id}/turns")
            assert response.status_code == 200, f"Expected 200, got {response.status_code}: {response.text}"
            body = response.json()
            assert "session_id" in body
            assert "turns" in body
            assert len(body["turns"]) == 2
            assert body["turns"][0]["child_id"] == child_id

    Add test in tests/api/test_session_end.py (append to existing file, do NOT rewrite the file):
        async def test_end_session_extracts_interests(test_client, test_data):
            """CURR-03: POST /sessions/{id}/end calls extract_and_update_interests"""
            child_id, session_id = test_data
            # Log a turn where a curriculum tag appears in the answer 2+ times
            # Use a known tag from CURRICULUM — "volcano" or "magma" are safe bets
            # Log the additional turns via the test_client or directly via db if needed
            response = await test_client.post(f"/v1/sessions/{session_id}/end")
            assert response.status_code == 200
            body = response.json()
            assert "session_id" in body
            # Interest extraction is best-effort; verify the endpoint did not crash
            # Full interest content test is covered in tests/services/test_session_intelligence.py
  </action>

  <verify>
    <automated>pytest tests/api/test_session_turns.py tests/api/test_session_end.py -x -v</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/api/test_session_turns.py -x -q` exits 0
    - `pytest tests/api/test_session_end.py -x -q` exits 0 (existing + new test)
    - `GET /v1/sessions/{session_id}/turns` returns 200 + body with "session_id" and "turns" keys
    - `grep -c "extract_and_update_interests" api/sessions.py` returns at least 2 (import + call)
    - `grep -c "get_session_turns" api/sessions.py` returns at least 2 (decorator line + function def)
    - `pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>

  <done>HIST-03 endpoint exists at GET /v1/sessions/{session_id}/turns and returns turns for the given session. POST /sessions/{id}/end triggers extract_and_update_interests() after BKT/FSRS updates. Both changes use the db session already in scope — no new dependency injection. CR-01 non-regression confirmed (no DB calls inside streaming generator).</done>
</task>

<task type="execute">
  <name>Task 3: Wire intelligence context calls into api/chat.py (HIST-01, HIST-02, CURR-01, CURR-02, CURR-03 catch-up, D-06, D-07, D-08)</name>
  <files>api/chat.py, tests/services/test_session_intelligence.py</files>

  <read_first>
    - api/chat.py — read the FULL file before editing; specifically: (a) the existing import block (lines 1–15); (b) the `chat()` function body — find: where `child` is resolved (get_child_by_id or get_child_by_device_id call); the `mastery_ctx` assignment; the `build_system_prompt()` call; the `log_turn()` call and what parameters it receives (especially `correct` and `kc_id`); (c) confirm the parameter name used for the DB session (`session` or `db`) in chat()
    - services/session_intelligence.py — confirm the exact names of exported functions: `build_24hr_history_context`, `build_prereq_tree_context`, `get_session_prereq_state`, `increment_prereq_turn`, `reset_prereq_turn`, `extract_and_update_interests`
    - db/crud.py — confirm `get_most_recent_ended_session` signature (from Plan 01): `async def get_most_recent_ended_session(child_id: str, session: AsyncSession) -> Optional[SessionModel]`
  </read_first>

  <action>
    Extend api/chat.py import block — add after the `from services.knowledge_tracing import mastery_context_for_prompt` line:

        from services.session_intelligence import (
            build_24hr_history_context,
            build_prereq_tree_context,
            get_session_prereq_state,
            increment_prereq_turn,
            reset_prereq_turn,
            extract_and_update_interests,
        )
        from db.crud import get_most_recent_ended_session

    In the `chat()` function body, find the block where `mastery_ctx` is built and `build_system_prompt()` is called. Match the existing DB session parameter name in that function (it may be `session` or `db`). Insert all new context calls between the `mastery_ctx` assignment and the `build_system_prompt()` call:

        mastery_ctx = await mastery_context_for_prompt(child_id, session, limit=5)

        # Phase 3 session intelligence (HIST-01, HIST-02, CURR-02)
        history_ctx = await build_24hr_history_context(child_id, session)
        prereq_tree = await build_prereq_tree_context(child_id, session, limit=5)
        prereq_state = get_session_prereq_state(child_id)

        # D-07: advance escalation counter for each unmet prereq on this turn
        for _entry in (prereq_tree or []):
            increment_prereq_turn(child_id, _entry["prereq_kc_id"])

        # D-08 catch-up: extract interests from previous session that missed /end call
        prev_session = await get_most_recent_ended_session(child_id, session)
        if prev_session and prev_session.id:
            await extract_and_update_interests(prev_session.id, child_id, session)

        system_prompt = await build_system_prompt(
            child,
            mastery_context=mastery_ctx or None,
            history_context=history_ctx or None,
            prereq_tree=prereq_tree or None,
            session_prereq_state=prereq_state or None,
        )

    Critical: ALL new DB/service calls go BEFORE `build_system_prompt()`. build_system_prompt() must remain a pure formatter receiving pre-fetched data — it must NOT call DB itself (RESEARCH.md Anti-Pattern 1).

    The `increment_prereq_turn` loop advances the D-07 counter once per chat turn for each unmet prerequisite present in prereq_tree. The counter is keyed by `entry["prereq_kc_id"]` — the KC ID set by build_prereq_tree_context() in Plan 03. This is the missing wire that makes the Turn 1 → Turn 2 → Turn 3+ escalation cadence actually advance.

    The catch-up trigger (D-08) calls extract_and_update_interests idempotently — if the previous session already had interests extracted (via /end), update_interests() will silently merge with no duplicates.

    D-06 reset wiring — find the code path in chat() that calls `log_turn(... correct=True ...)`. After that call, insert the reset trigger for correct prereq answers:
        if correct and kc_id and kc_id in [_e.get("prereq_kc_id") for _e in (prereq_tree or [])]:
            reset_prereq_turn(child_id, kc_id)
    This implements D-06: when a correct probe answer is received for a prerequisite KC, the escalation counter is cleared so the tutor can re-engage fresh on subsequent references.

    Use the same DB session parameter name that already exists in the chat() function body — do not introduce a second DB session.

    Note on CURR-01: next_topics() already factors in child.interests when building candidates (Phase 2 implementation). Wiring history_ctx and prereq_tree into build_system_prompt() completes CURR-01 — the system prompt now guides the tutor toward topics surfaced by next_topics() while also showing prerequisite gaps.

    Integration test (add to tests/services/test_session_intelligence.py — append, do NOT rewrite):

    Add test: test_prereq_tree_to_system_prompt_pipeline
    - Create child and session in db_session
    - Identify a Topic from CURRICULUM that has prerequisites with p_mastery below "solid"
    - Call `prereq_tree = await build_prereq_tree_context(child_id, db_session)` — assert len >= 0 (may be empty if child has no eligible topics; proceed only if non-empty)
    - If prereq_tree is non-empty: call `increment_prereq_turn(child_id, prereq_tree[0]["prereq_kc_id"])`; then call `state = get_session_prereq_state(child_id)`; assert state[prereq_tree[0]["prereq_kc_id"]] == 1
    - Call `result = await build_system_prompt(_make_child(), prereq_tree=prereq_tree, session_prereq_state=state)`
    - Assert: "Turn 1" in result or "engage" in result (escalation signal present in prompt output)
    - This exercises the full build_prereq_tree_context() → increment_prereq_turn() → get_session_prereq_state() → build_system_prompt() pipeline
  </action>

  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/ -x -q` exits 0 (full suite green after wiring)
    - `pytest tests/evals/ -x -q` exits 0 (evals unaffected by new prompt context)
    - `grep -c "build_24hr_history_context" api/chat.py` returns at least 2 (import + call)
    - `grep -c "build_prereq_tree_context" api/chat.py` returns at least 2 (import + call)
    - `grep -c "increment_prereq_turn" api/chat.py` returns at least 2 (import + call)
    - `grep -c "get_most_recent_ended_session" api/chat.py` returns at least 2 (import + call)
    - `grep -c "history_context=history_ctx" api/chat.py` returns 1 (new build_system_prompt call)
    - All new calls appear BEFORE the `build_system_prompt(` call in the chat() function body
    - `pytest tests/services/test_session_intelligence.py -k test_prereq_turn_increment_on_chat -x -q` exits 0 (pipeline integration test passes)
  </acceptance_criteria>

  <done>api/chat.py calls all Phase 3 service functions before build_system_prompt(). increment_prereq_turn() is called for each entry in prereq_tree on every chat turn — the D-07 escalation counter now advances. reset_prereq_turn() fires on correct prereq probe answers (D-06). The system prompt for a child with 24h history includes the "Recent topics:" block. The D-08 catch-up trigger runs on every chat() call — idempotent, no effect when no prior session exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → api/chat.py | child_id derived from device_id or auth header; session content is child answer text |
| HTTP client → api/sessions.py | session_id is a path param; unauthenticated (CR-02 pre-existing) |
| api/chat.py → build_system_prompt() | Prompt content includes child interests/history; injects into LLM system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04-01 | Information Disclosure | GET /sessions/{session_id}/turns (HIST-03) | accept | Same IDOR exposure as existing GET /sessions/{child_id} (CR-02). Both endpoints fixed together in Phase 4 auth work. Do NOT add inconsistent auth check here only. |
| T-3-04-02 | Information Disclosure | build_system_prompt with history/prereq context | accept | System prompt is sent to LLM API (Haiku) only — not returned to HTTP client. Prompt content includes topic names (not raw answers). Acceptable under existing trust model. |
| T-3-04-03 | Elevation of Privilege | extract_and_update_interests in chat() catch-up path | mitigate | catch-up runs for `get_most_recent_ended_session(child_id)` — child_id is resolved from trusted device_id lookup before this call. No caller-supplied session_id used in catch-up path. |
| T-3-04-04 | Denial of Service | Three new async DB calls on every chat() request | accept | All three calls are simple indexed reads (child_id indexed). Latency impact is minimal per RESEARCH.md analysis (~3ms total for a typical child). Acceptable for Phase 3 dev target. |
| T-3-04-05 | Tampering | prereq_tree block in system prompt | accept | Content is Topic.name strings from static CURRICULUM — never user-supplied text. No injection vector into the LLM system prompt. |
| T-3-04-SC | Tampering | No new package installs in this plan | accept | Phase 3 introduces no new external packages. Package legitimacy gate not applicable. |
</threat_model>

<verification>
- `pytest tests/ -x -q` — full suite green (unit + integration + evals)
- `pytest tests/api/test_session_turns.py -v` — HIST-03 integration test passes
- `pytest tests/api/test_session_end.py -v` — existing + new end session tests pass
- `pytest tests/services/test_tutor.py -v` — build_system_prompt backward-compat + new param tests pass
- `pytest tests/services/test_session_intelligence.py -k test_prereq_tree_to_system_prompt_pipeline -v` — pipeline integration test passes
- Manual spot check: `grep "history_context=" api/chat.py` confirms wiring; `grep "increment_prereq_turn" api/chat.py` confirms counter advancement; `grep "extract_and_update_interests" api/sessions.py` confirms fast-path wiring
</verification>

<success_criteria>
All seven Phase 3 requirements are implemented and wired:
- HIST-01: system prompt includes 24hr history block when child has recent events
- HIST-02: system prompt includes prereq tree block when child has fragile/not_started prerequisites
- HIST-03: GET /sessions/{session_id}/turns endpoint returns 200 + turn list
- CURR-01: next_topics() + child.interests drives topic selection; prompt enrichment guides the tutor
- CURR-02: rubber-band escalation counter advances via increment_prereq_turn() on every chat turn; session_prereq_state flows into build_system_prompt() for D-07 turn-calibrated signals
- CURR-03: interest extraction runs at session end (fast path) and session start (catch-up path)
- CURR-04: supersedes unlock wired via Plan 02 (already in next_topics(), no additional wiring needed)
Full test suite green. Evals unaffected. CR-01/CR-02 non-regressions confirmed.
</success_criteria>

<output>
Create `.planning/phases/03-session-intelligence/03-04-SUMMARY.md` when done.
</output>
