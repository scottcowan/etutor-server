---
phase: 03-session-intelligence
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - db/crud.py
  - tests/db/test_crud_session_intelligence.py
autonomous: true
requirements:
  - HIST-03

must_haves:
  truths:
    - "get_24hr_history() returns only events within the past 24 hours for the given child_id, in DESC timestamp order"
    - "get_turns_by_session_id() returns only events for the given session_id, in ASC timestamp order"
    - "get_most_recent_ended_session() returns the most recent SessionModel row where ended_at IS NOT NULL for the given child_id"
    - "All three functions return empty list / None cleanly when no matching rows exist"
  artifacts:
    - path: "db/crud.py"
      provides: "get_24hr_history, get_turns_by_session_id, get_most_recent_ended_session"
      contains: "async def get_24hr_history"
    - path: "tests/db/test_crud_session_intelligence.py"
      provides: "TDD test coverage for all three new CRUD functions"
      contains: "test_get_24hr_history_excludes_old_events"
  key_links:
    - from: "services/session_intelligence.py"
      to: "db/crud.py"
      via: "get_24hr_history, get_turns_by_session_id"
      pattern: "get_24hr_history|get_turns_by_session_id"
    - from: "api/chat.py"
      to: "db/crud.py"
      via: "get_most_recent_ended_session"
      pattern: "get_most_recent_ended_session"
---

<objective>
Add three new CRUD functions to db/crud.py that provide the data-access foundation for Phase 3 session intelligence. These functions will be called by services/session_intelligence.py (Plans 03-03 and 03-04) to build 24-hour history context, retrieve session turns, and support the D-08 dual-trigger interest extraction catch-up path.

Purpose: Lay the data layer before the service and wiring layers build on top of it.
Output: Three new async functions in db/crud.py + a passing TDD test file.
</objective>

<execution_context>
@/Users/scowan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/scowan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-intelligence/03-CONTEXT.md
@.planning/phases/03-session-intelligence/03-PATTERNS.md
</context>

<feature>
  <name>Phase 3 CRUD Data Layer — get_24hr_history, get_turns_by_session_id, get_most_recent_ended_session</name>

  <files>db/crud.py, tests/db/test_crud_session_intelligence.py</files>

  <read_first>
    - db/crud.py — read the full file before any edits; note the import block (add `timedelta` to the `from datetime import datetime, timezone` line at line 19), the `get_session_history()` function (lines 161–178) as the exact pattern to follow, and the `SessionModel` import already present
    - db/models.py — read to confirm: InteractionEventModel fields (child_id, session_id, topic, answer, timestamp); SessionModel fields (id, child_id, started_at, ended_at)
    - tests/db/test_crud_events.py — read to understand the async test fixture pattern (`db_session` fixture, `create_child`, `log_turn` imports) that the new test file must replicate
  </read_first>

  <behavior>
    ## RED Phase — Write these failing tests in tests/db/test_crud_session_intelligence.py

    Test 1 — get_24hr_history_excludes_old_events:
    - Create child, log two turns: one with timestamp=now-25h (via direct model insert with explicit timestamp), one with timestamp=now-1h
    - Call get_24hr_history(child_id, db_session)
    - Assert: result contains exactly 1 event (the -1h one); the -25h event is excluded
    - Assert: result[0].child_id == child_id

    Test 2 — get_24hr_history_returns_desc_order:
    - Create child, log three turns with timestamps: now-3h, now-6h, now-1h (inserted in that order)
    - Call get_24hr_history(child_id, db_session)
    - Assert: result[0].timestamp > result[1].timestamp > result[2].timestamp (DESC order)

    Test 3 — get_24hr_history_empty_for_no_events:
    - Create child with no events
    - Call get_24hr_history(child_id, db_session)
    - Assert: result == []

    Test 4 — get_turns_by_session_id_scoped:
    - Create child; create two sessions (session_a, session_b); log 2 turns under session_a and 1 turn under session_b (all using log_turn with session_id param)
    - Call get_turns_by_session_id(session_a.id, db_session)
    - Assert: result has exactly 2 turns; all have session_id == session_a.id

    Test 5 — get_turns_by_session_id_asc_order:
    - Create child; create session; log turns with timestamps now-5m and now-2m
    - Call get_turns_by_session_id(session.id, db_session)
    - Assert: result[0].timestamp < result[1].timestamp (ASC order)

    Test 6 — get_most_recent_ended_session_returns_ended:
    - Create child; create session_a (ended, ended_at=now-2h); create session_b (open, ended_at=None)
    - Call get_most_recent_ended_session(child_id, db_session)
    - Assert: result.id == session_a.id

    Test 7 — get_most_recent_ended_session_returns_none_when_no_ended:
    - Create child; create session with ended_at=None
    - Call get_most_recent_ended_session(child_id, db_session)
    - Assert: result is None

    ## GREEN Phase — Implement in db/crud.py

    Function 1: get_24hr_history(child_id, session, limit=50)
    - Add `timedelta` to the existing `from datetime import datetime, timezone` import
    - since = datetime.now(timezone.utc) - timedelta(hours=24) — use timezone.utc, never datetime.utcnow()
    - SQLAlchemy select(InteractionEventModel).where(child_id==).where(timestamp>=since).order_by(timestamp.desc()).limit(limit)
    - Return list(result.scalars().all()) — DESC order, no reversal needed (callers consume DESC)
    - Docstring must include: "HIST-01 (D-03): UTC-aware datetime comparison — never datetime.utcnow()"
    - Security comment: "child_id parameterised via SQLAlchemy ORM"

    Function 2: get_turns_by_session_id(session_id, db)
    - Parameter named `db` (not `session`) to avoid confusion with SessionModel rows — matches end_session() convention
    - SQLAlchemy select(InteractionEventModel).where(session_id==).order_by(timestamp.asc())
    - Return list(result.scalars().all()) — ASC order for replay
    - Docstring must include: "HIST-03: Same IDOR exposure as get_session_history() — fix both together in auth phase (CR-02)"

    Function 3: get_most_recent_ended_session(child_id, session)
    - SQLAlchemy select(SessionModel).where(child_id==).where(SessionModel.ended_at.isnot(None)).order_by(SessionModel.ended_at.desc()).limit(1)
    - Return single row or None: use `result.scalars().first()`
    - Docstring must include: "D-08: catch-up interest extraction trigger"

    ## REFACTOR Phase

    - Verify docstrings are consistent with existing crud.py style (short imperative description, security notes where applicable)
    - Verify all three functions follow `select/where/order_by/limit` pattern from get_session_history()
    - No functional changes needed; refactor is style/doc only
  </behavior>

  <implementation>
    Place get_24hr_history() and get_turns_by_session_id() immediately after get_session_history() (after line 178).
    Place get_most_recent_ended_session() in the SessionModel section of crud.py — find the session CRUD block (after the InteractionEvent block).
    All three are pure read functions — no commit(), no refresh() calls needed.
  </implementation>

  <acceptance_criteria>
    - `pytest tests/db/test_crud_session_intelligence.py -x -q` exits 0 (all 7 tests pass)
    - `pytest tests/ -x -q` exits 0 (no regressions in existing suite)
    - db/crud.py contains `async def get_24hr_history(`
    - db/crud.py contains `async def get_turns_by_session_id(`
    - db/crud.py contains `async def get_most_recent_ended_session(`
    - db/crud.py import line for datetime includes `timedelta` (grep: `from datetime import.*timedelta`)
    - No `datetime.utcnow()` in the new functions (grep should find zero matches in new code)
  </acceptance_criteria>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API caller → db/crud.py | child_id and session_id are received from API path params; no direct external input reaches these CRUD functions but they are called with caller-supplied IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01-01 | Information Disclosure | get_turns_by_session_id | accept | Same IDOR exposure as existing get_session_history() (CR-02). No auth at this layer — fix both endpoints together in Phase 4 auth work. CR-02 non-regression: do not add inconsistent auth here. |
| T-3-01-02 | Information Disclosure | get_24hr_history | accept | child_id-scoped read; parameterised ORM query prevents cross-child data access. Same trust level as existing get_session_history(). |
| T-3-01-03 | Tampering | All three functions | mitigate | All WHERE clauses use SQLAlchemy ORM column comparisons — never string interpolation. Parameterised queries throughout. |
| T-3-01-SC | Tampering | No new package installs in this plan | accept | Phase 3 introduces no new external packages. Package legitimacy gate not applicable. |
</threat_model>

<verification>
- `pytest tests/db/test_crud_session_intelligence.py -v` — all 7 tests pass
- `pytest tests/ -x -q` — full suite green, no regressions
- `grep -c "async def get_24hr_history" db/crud.py` returns 1
- `grep -c "async def get_turns_by_session_id" db/crud.py` returns 1
- `grep -c "async def get_most_recent_ended_session" db/crud.py` returns 1
</verification>

<success_criteria>
Three async CRUD functions exist in db/crud.py and pass TDD tests:
1. get_24hr_history filters by UTC-aware 24h cutoff and returns events DESC by timestamp
2. get_turns_by_session_id returns session-scoped events ASC by timestamp
3. get_most_recent_ended_session returns the most recent ended SessionModel or None
Full test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/03-session-intelligence/03-01-SUMMARY.md` when done.
</output>
