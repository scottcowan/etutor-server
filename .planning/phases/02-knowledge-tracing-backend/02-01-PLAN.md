---
phase: 02-knowledge-tracing-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - db/models.py
  - db/crud.py
  - services/sessions.py
  - migrations/versions/{autogenerated}_add_child_fsrs_params.py
autonomous: false
requirements:
  - KT-01
  - KT-02
  - KT-04
user_setup: []

must_haves:
  truths:
    - "ChildFSRSParamsModel exists in db/models.py with child_id PK, weights JSON column, updated_at"
    - "get_child_fsrs_params() and upsert_child_fsrs_params() are callable from db/crud.py"
    - "log_turn() in db/crud.py accepts kc_id and correct as keyword-only optional params"
    - "alembic upgrade head succeeds on a fresh SQLite file — child_fsrs_params table is created"
    - "fsrs[optimizer]>=6.3.1 is present in requirements.txt"
  artifacts:
    - path: "db/models.py"
      provides: "ChildFSRSParamsModel ORM class"
      contains: "class ChildFSRSParamsModel"
    - path: "db/crud.py"
      provides: "FSRS params CRUD + kc_id/correct in log_turn"
      contains: "upsert_child_fsrs_params"
    - path: "requirements.txt"
      provides: "fsrs dependency"
      contains: "fsrs"
    - path: "migrations/versions/{hash}_add_child_fsrs_params.py"
      provides: "DB migration for child_fsrs_params"
      contains: "child_fsrs_params"
  key_links:
    - from: "db/models.py ChildFSRSParamsModel"
      to: "child_profiles.id"
      via: "ForeignKey"
      pattern: "ForeignKey.*child_profiles.id"
    - from: "db/crud.py log_turn"
      to: "InteractionEventModel.kc_id"
      via: "keyword arg"
      pattern: "kc_id.*InteractionEventModel"
---

<objective>
Bootstrap the Phase 2 data layer: add the `child_fsrs_params` table (D-06), extend `log_turn()` to
accept `kc_id` and `correct` parameters (prerequisite for BKT batch update — KT-04), and install
the `fsrs` library (D-04). Without this plan, every subsequent KT plan has missing imports.

Purpose: Prevent "Pitfall 1: kc_id Never Gets Set" — the most likely silent failure mode in Phase 2.
Output: DB model + CRUD + Alembic migration + requirements entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-knowledge-tracing-backend/02-CONTEXT.md
@.planning/phases/02-knowledge-tracing-backend/02-RESEARCH.md
@.planning/phases/02-knowledge-tracing-backend/02-PATTERNS.md
@db/models.py
@db/crud.py
@services/sessions.py
@migrations/versions/9f229414e92b_initial_schema.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify fsrs package legitimacy before install</name>
  <read_first>
    .planning/phases/02-knowledge-tracing-backend/02-RESEARCH.md — "Package Legitimacy Audit" section
  </read_first>
  <what-built>
    The research phase verified fsrs 6.3.1 manually (PyPI registry, source repo, MIT license,
    3.5yr history) but slopcheck was unavailable — package is tagged [ASSUMED]. This checkpoint
    confirms legitimacy before the install task adds it to requirements.txt.
  </what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/fsrs/ — confirm package exists, owner is
       "open-spaced-repetition", version >= 6.3.1, MIT license.
    2. Visit https://github.com/open-spaced-repetition/py-fsrs — confirm active repo,
       recent commits within last 6 months, README documents Scheduler/Card/Rating/Optimizer API.
    3. Confirm no name-squatting indicators: the PyPI description matches the GitHub README.
  </how-to-verify>
  <resume-signal>Type "fsrs approved" to proceed, or describe any issues found.</resume-signal>
  <acceptance_criteria>
    Human has confirmed fsrs package on PyPI matches github.com/open-spaced-repetition/py-fsrs
    and is safe to install.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add ChildFSRSParamsModel, FSRS CRUD, extend log_turn, update requirements</name>
  <read_first>
    db/models.py — read entirely before editing; note imports block (line 15-16) and MasteryStateModel pattern
    db/crud.py — read entirely; note log_turn signature (line 125-150) and create_or_get_mastery_state pattern (line 177-190)
    services/sessions.py — read entirely; note log_turn wrapper (line 21-29)
    .planning/phases/02-knowledge-tracing-backend/02-PATTERNS.md — "ChildFSRSParamsModel" and "db/crud.py" sections
    .planning/phases/02-knowledge-tracing-backend/02-RESEARCH.md — "ChildFSRSParamsModel Definition" and "Pattern: Upsert child_fsrs_params" sections
  </read_first>
  <files>
    requirements.txt,
    db/models.py,
    db/crud.py,
    services/sessions.py
  </files>
  <action>
    Make four independent changes in one pass:

    1. REQUIREMENTS.TXT — append after alembic line (preserve existing version pins):
       `fsrs[optimizer]>=6.3.1`
       The `[optimizer]` extra is required for `fit_fsrs_params()` per D-05. torch is a
       transitive dependency — acceptable for server-side use.

    2. DB/MODELS.PY — add ChildFSRSParamsModel class after MasteryStateModel. No new imports
       needed (JSON, String, DateTime, ForeignKey, Mapped, mapped_column already imported).
       Fields: child_id (String, FK child_profiles.id, PK), weights (JSON, nullable=False),
       updated_at (DateTime(timezone=True), default=lambda: datetime.now(timezone.utc)).
       Follow the JSON column pattern from ChildProfileModel.interests (line 32-33).
       Do NOT add any relationship() declarations — project does not use them.
       Do NOT use String for weights — use JSON column type so SQLAlchemy handles list serialisation.

    3. DB/CRUD.PY — make two additions:
       a) Extend log_turn() to accept two new keyword-only params after `session_id`:
          `kc_id: Optional[str] = None` and `correct: Optional[bool] = None`.
          Pass them into the InteractionEventModel constructor: `kc_id=kc_id, correct=correct`.
          This fixes the critical gap where BKT batch update would find zero events (Pitfall 1 in RESEARCH.md).
          The params are keyword-only (after the existing `session_id` kwarg) — existing callers
          that pass session_id as keyword are unaffected.
       b) Add two new CRUD functions after the MasteryState section:
          `get_child_fsrs_params(child_id: str, session: AsyncSession) -> Optional[ChildFSRSParamsModel]`
          — use `await session.get(ChildFSRSParamsModel, child_id)` (direct PK lookup, no select needed).
          `upsert_child_fsrs_params(child_id: str, weights: list, session: AsyncSession) -> None`
          — follow the get-or-create + conditional-update pattern from PATTERNS.md:
          session.get → if exists update weights+updated_at else session.add new model → await session.commit().
          Import ChildFSRSParamsModel at the top of crud.py alongside the existing model imports.

    4. SERVICES/SESSIONS.PY — extend log_turn() wrapper to forward the new params:
       Add `kc_id: str = None` and `correct: bool = None` to the signature.
       Pass them as `kc_id=kc_id, correct=correct` in the db_log_turn() call.
       This keeps the thin-wrapper pattern consistent.

    CRITICAL: Do NOT modify api/chat.py in this plan — that wiring happens in Plan 02-06.
    Do NOT add any imports to crud.py beyond ChildFSRSParamsModel.
  </action>
  <verify>
    <automated>
      python -c "from db.models import ChildFSRSParamsModel; print('ChildFSRSParamsModel OK')"
      python -c "from db.crud import upsert_child_fsrs_params, get_child_fsrs_params, log_turn; import inspect; sig = inspect.signature(log_turn); assert 'kc_id' in sig.parameters and 'correct' in sig.parameters, 'log_turn missing kc_id/correct'; print('CRUD OK')"
      grep -c "fsrs\[optimizer\]" requirements.txt
    </automated>
  </verify>
  <done>
    ChildFSRSParamsModel importable from db.models; upsert_child_fsrs_params importable from db.crud;
    log_turn in db.crud has kc_id and correct params; requirements.txt contains fsrs[optimizer]>=6.3.1.
  </done>
  <acceptance_criteria>
    - `from db.models import ChildFSRSParamsModel` succeeds with no ImportError
    - `inspect.signature(db.crud.log_turn)` shows kc_id and correct parameters
    - `upsert_child_fsrs_params` and `get_child_fsrs_params` importable from db.crud
    - `grep "fsrs" requirements.txt` returns `fsrs[optimizer]>=6.3.1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Generate and verify Alembic migration for child_fsrs_params</name>
  <read_first>
    migrations/env.py — confirm `from db.models import Base` is present (it is per PATTERNS.md)
    migrations/versions/9f229414e92b_initial_schema.py — note the revision ID for down_revision
    .planning/phases/02-knowledge-tracing-backend/02-PATTERNS.md — "migrations/versions/" section
    .planning/phases/02-knowledge-tracing-backend/02-RESEARCH.md — "Alembic Migration" section
  </read_first>
  <files>
    migrations/versions/{autogenerated}_add_child_fsrs_params.py
  </files>
  <action>
    Run: `alembic revision --autogenerate -m "add child_fsrs_params"`

    This creates a new migrations/versions/{hash}_add_child_fsrs_params.py file. Verify the
    generated file contains:
    - `down_revision = '9f229414e92b'` (pointing to the initial schema)
    - `op.create_table('child_fsrs_params', ...)` with columns: child_id (String, PK), weights (JSON),
      updated_at (DateTime), ForeignKeyConstraint on child_profiles.id
    - `op.drop_table('child_fsrs_params')` in downgrade()

    If the autogenerated file uses `sa.String()` for weights instead of `sa.JSON()`, manually
    correct it to `sa.JSON()` — SQLAlchemy sometimes renders JSON columns as String in autogenerate.

    Then run: `alembic upgrade head`

    Verify it completes without errors. Then run:
    `alembic downgrade -1` followed by `alembic upgrade head`
    to confirm the migration is fully reversible.

    IMPORTANT: Do NOT run `alembic stamp head` — the DB may already be at the initial migration.
    Run `alembic current` first to see the current revision. If it shows `9f229414e92b`, run
    `alembic upgrade head` directly. If it shows `head` already (shouldn't happen), something
    is wrong — investigate before proceeding.
  </action>
  <verify>
    <automated>
      alembic current
      alembic upgrade head
      python -c "
import asyncio
from sqlalchemy.ext.asyncio import create_async_engine
from sqlalchemy import text

async def check():
    engine = create_async_engine('sqlite+aiosqlite:///:memory:')
    from db.models import Base
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    async with engine.connect() as conn:
        result = await conn.execute(text(\"SELECT name FROM sqlite_master WHERE type='table' AND name='child_fsrs_params'\"))
        row = result.fetchone()
        assert row is not None, 'child_fsrs_params table not found'
        print('Migration OK — child_fsrs_params table exists')
    await engine.dispose()

asyncio.run(check())
"
    </automated>
  </verify>
  <done>
    `alembic upgrade head` runs cleanly; child_fsrs_params table exists in the DB;
    `alembic downgrade -1` + `alembic upgrade head` round-trips without error.
  </done>
  <acceptance_criteria>
    - `alembic upgrade head` exits 0 with no error output
    - `alembic current` shows the new migration as the current revision
    - In-memory SQLite created from Base.metadata.create_all has a child_fsrs_params table
    - Migration file has down_revision pointing to 9f229414e92b
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install → requirements.txt | External package pulled from PyPI; supply-chain attack surface |
| service layer → db/crud.py log_turn | kc_id/correct values originate from caller context; must be parameterised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | db/crud.py log_turn kc_id param | mitigate | SQLAlchemy ORM parameterised INSERT — never string-interpolated; follow established select() pattern from crud.py security note |
| T-2-02 | Tampering | db/crud.py upsert_child_fsrs_params | mitigate | session.get() by PK (parameterised); weights written as Python list via JSON column — no string injection surface |
| T-2-SC | Tampering | fsrs[optimizer] pip install | mitigate | slopcheck + blocking human checkpoint (Task 1) for [ASSUMED] package; human must approve before Task 2 runs |
</threat_model>

<verification>
After all tasks complete:
1. `python -c "from db.models import ChildFSRSParamsModel, MasteryStateModel; print('OK')"` — no ImportError
2. `python -c "from db.crud import log_turn, upsert_child_fsrs_params, get_child_fsrs_params; print('OK')"` — no ImportError
3. `alembic upgrade head` — exits 0
4. `alembic downgrade -1 && alembic upgrade head` — round-trip succeeds
5. `grep -c "fsrs" requirements.txt` — returns at least 1
6. `pytest tests/ -x --ignore=tests/evals` — existing tests still pass (log_turn signature change is backward-compatible)
</verification>

<success_criteria>
- ChildFSRSParamsModel in db/models.py with JSON weights column and FK to child_profiles
- upsert_child_fsrs_params and get_child_fsrs_params in db/crud.py
- log_turn in db/crud.py and services/sessions.py accepts kc_id and correct keyword args
- Alembic migration creates child_fsrs_params table; upgrade+downgrade round-trips cleanly
- fsrs[optimizer]>=6.3.1 in requirements.txt
- All existing pytest tests still pass
</success_criteria>

<output>
Create `.planning/phases/02-knowledge-tracing-backend/02-01-SUMMARY.md` when done.
</output>
